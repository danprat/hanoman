# Auth (SPEC-169) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Tambah auth email/password ke hanoman: login, invite user (set password langsung, tanpa email), ganti password; semua `/api/**` terproteksi sesi.

**Architecture:** Sesi = token opaque acak (256-bit) di cookie `httpOnly`; mapping-nya (`sha256(token)` → user) di tabel Postgres `Session` → bisa dicabut. Password di-hash `crypto.scrypt` (stdlib). Enforcement lewat satu `onRequest` hook di scope `/api`. Frontend gate seluruh app di balik layar Login/Setup.

**Tech Stack:** Fastify + `@fastify/cookie`, Prisma/Postgres, zod, `node:crypto` (scrypt/randomBytes/timingSafeEqual/createHash), React (SPA existing).

## Global Constraints

- TypeScript strict; test untuk setiap logika orkestrasi.
- Tanpa RBAC — semua user setara (verbatim brief: "tidak ada RBAC, semua role sama").
- Password `min(8)`; email `unique`, format tervalidasi.
- Cookie: `httpOnly`, `sameSite:"strict"`, `secure: NODE_ENV==="production"`, `path:"/"`, `maxAge` 7 hari, nama `hn_session`.
- `UserView` TAK PERNAH membawa `passwordHash`.
- Tanpa env baru wajib. HOST default tetap `127.0.0.1`.
- Perbarui `internal/docs/**` yang tersentuh dalam commit yang sama.
- Test repo: `env -u NODE_ENV -u DATABASE_URL vitest run --no-file-parallelism` (lihat memory shell-env-points-at-prod).

---

### Task 1: Skema Prisma + migration

**Files:**
- Modify: `server/prisma/schema.prisma`
- Create: `server/prisma/migrations/<ts>_add_auth/migration.sql` (via CLI)

**Interfaces:**
- Produces: model `User { id, email(unique), passwordHash, createdAt, sessions }`, model `Session { id, userId, createdAt, expiresAt, user }` dengan `onDelete: Cascade`.

- [x] **Step 1:** Tambah ke `server/prisma/schema.prisma`:

```prisma
model User {
  id           String    @id @default(cuid())
  email        String    @unique
  passwordHash String
  createdAt    DateTime  @default(now())
  sessions     Session[]
}

model Session {
  id        String   @id // sha256(token) hex; token asli hanya di cookie
  userId    String
  createdAt DateTime @default(now())
  expiresAt DateTime
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
}
```

- [x] **Step 2:** Buat migration + generate client (worktree perlu install dulu — lihat memory worktree-needs-install-and-generate):

```bash
cd server && pnpm exec prisma migrate dev --name add_auth
```
Expected: migration `<ts>_add_auth` dibuat, client ter-generate, `prisma.user`/`prisma.session` tersedia.

- [x] **Step 3:** Migrate DB test (kalau belum): `DATABASE_URL=$TEST_DATABASE_URL pnpm exec prisma migrate deploy` (default `hanoman_test`).

- [x] **Step 4: Commit** `git add server/prisma && git commit -m "feat(db): model User + Session untuk auth (SPEC-169)"`

---

### Task 2: Auth service (`services/auth.ts`) + unit test

**Files:**
- Create: `server/src/services/auth.ts`
- Create: `server/test/auth-service.test.ts`

**Interfaces:**
- Consumes: `prisma` dari `../db`.
- Produces:
  - `hashPassword(pw: string): Promise<string>` — `"<saltHex>:<hashHex>"`.
  - `verifyPassword(pw: string, stored: string): Promise<boolean>` — timing-safe.
  - `newSessionToken(): string`, `sessionId(token: string): string` (= sha256 hex).
  - `createSession(userId: string): Promise<string>` — return **token asli** (bukan id).
  - `lookupSession(token: string): Promise<{ id, email, createdAt } | null>` — null bila expired/absen.
  - `deleteSession(token: string): Promise<void>`.
  - `deleteUserSessions(userId: string, exceptToken?: string): Promise<void>`.
  - `loginThrottle(ip: string): { blocked: boolean }`, `noteLoginFail(ip: string): void`, `clearLoginFails(ip: string): void`.
  - Konstanta: `COOKIE_NAME = "hn_session"`, `SESSION_TTL_MS = 7*24*3600*1000`, `cookieOpts(): {...}`.

- [x] **Step 1: Test dulu** — `server/test/auth-service.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { hashPassword, verifyPassword, sessionId, newSessionToken } from "../src/services/auth";

describe("auth service", () => {
  it("hash round-trips and rejects wrong password", async () => {
    const h = await hashPassword("correct horse");
    expect(h).toMatch(/^[0-9a-f]+:[0-9a-f]+$/);
    expect(await verifyPassword("correct horse", h)).toBe(true);
    expect(await verifyPassword("wrong", h)).toBe(false);
  });
  it("sessionId is deterministic sha256 hex of token", () => {
    const t = newSessionToken();
    expect(sessionId(t)).toHaveLength(64);
    expect(sessionId(t)).toBe(sessionId(t));
    expect(sessionId(newSessionToken())).not.toBe(sessionId(t));
  });
});
```

- [x] **Step 2:** Run `env -u NODE_ENV -u DATABASE_URL pnpm --filter ./server exec vitest run test/auth-service.test.ts` → FAIL (module belum ada).

- [x] **Step 3:** Implement `server/src/services/auth.ts`:

```ts
import { randomBytes, scrypt as scryptCb, timingSafeEqual, createHash } from "node:crypto";
import { promisify } from "node:util";
import { prisma } from "../db";

const scrypt = promisify(scryptCb) as (pw: string, salt: Buffer, len: number) => Promise<Buffer>;

export const COOKIE_NAME = "hn_session";
export const SESSION_TTL_MS = 7 * 24 * 3600 * 1000;

export async function hashPassword(pw: string): Promise<string> {
  const salt = randomBytes(16);
  const key = await scrypt(pw, salt, 64);
  return `${salt.toString("hex")}:${key.toString("hex")}`;
}
export async function verifyPassword(pw: string, stored: string): Promise<boolean> {
  const [saltHex, hashHex] = stored.split(":");
  if (!saltHex || !hashHex) return false;
  const key = await scrypt(pw, Buffer.from(saltHex, "hex"), 64);
  const want = Buffer.from(hashHex, "hex");
  return key.length === want.length && timingSafeEqual(key, want);
}

export const newSessionToken = () => randomBytes(32).toString("base64url");
export const sessionId = (token: string) => createHash("sha256").update(token).digest("hex");

export async function createSession(userId: string): Promise<string> {
  const token = newSessionToken();
  await prisma.session.create({
    data: { id: sessionId(token), userId, expiresAt: new Date(Date.now() + SESSION_TTL_MS) },
  });
  return token;
}
export async function lookupSession(token: string) {
  const s = await prisma.session.findUnique({ where: { id: sessionId(token) }, include: { user: true } });
  if (!s) return null;
  if (s.expiresAt.getTime() < Date.now()) { await prisma.session.delete({ where: { id: s.id } }).catch(() => {}); return null; }
  return { id: s.user.id, email: s.user.email, createdAt: s.user.createdAt };
}
export async function deleteSession(token: string): Promise<void> {
  await prisma.session.deleteMany({ where: { id: sessionId(token) } });
}
export async function deleteUserSessions(userId: string, exceptToken?: string): Promise<void> {
  await prisma.session.deleteMany({
    where: { userId, ...(exceptToken ? { id: { not: sessionId(exceptToken) } } : {}) },
  });
}

// ponytail: throttle in-memory, reset saat restart, per-proses — cukup single VPS.
const fails = new Map<string, { n: number; until: number }>();
export function loginThrottle(ip: string): { blocked: boolean } {
  const f = fails.get(ip);
  return { blocked: !!f && f.until > Date.now() };
}
export function noteLoginFail(ip: string): void {
  const f = fails.get(ip) ?? { n: 0, until: 0 };
  f.n += 1;
  if (f.n >= 10) { f.until = Date.now() + 60_000; f.n = 0; }
  fails.set(ip, f);
}
export function clearLoginFails(ip: string): void { fails.delete(ip); }

export function cookieOpts() {
  return { httpOnly: true, sameSite: "strict" as const, secure: process.env.NODE_ENV === "production",
    path: "/", maxAge: SESSION_TTL_MS / 1000 };
}
```

- [x] **Step 4:** Run test → PASS.
- [x] **Step 5: Commit** `git add server/src/services/auth.ts server/test/auth-service.test.ts && git commit -m "feat(server): auth service — scrypt hash + sesi opaque (SPEC-169)"`

---

### Task 3: Kontrak shared (paths + dto)

**Files:**
- Modify: `shared/src/api.ts`
- Modify: `shared/src/dto.ts`

**Interfaces:**
- Produces:
  - `paths.authStatus/authSetup/authLogin/authLogout/authUsers`, `paths.authUser(id)`, `paths.authChangePassword`.
  - `zLogin` `{email,password}`, `zSignup` `{email,password}` (email().password min(8)), `zChangePassword` `{currentPassword,newPassword(min8)}`.
  - tipe `UserView = { id: string; email: string; createdAt: string }`, `AuthStatus = { needsSetup: boolean; user: UserView | null }`.

- [x] **Step 1:** Tambah di `shared/src/api.ts` (dalam objek `paths`):

```ts
  authStatus: `${API}/auth/status`,
  authSetup: `${API}/auth/setup`,
  authLogin: `${API}/auth/login`,
  authLogout: `${API}/auth/logout`,
  authUsers: `${API}/auth/users`,
  authUser: (id: string) => `${API}/auth/users/${id}`,
  authChangePassword: `${API}/auth/change-password`,
```

- [x] **Step 2:** Tambah di `shared/src/dto.ts` (ikuti pola zod existing di file itu; `import { z }` sudah ada):

```ts
export const zLogin = z.object({ email: z.string().email(), password: z.string().min(1) });
export const zSignup = z.object({ email: z.string().email(), password: z.string().min(8) });
export const zChangePassword = z.object({ currentPassword: z.string().min(1), newPassword: z.string().min(8) });
export type UserView = { id: string; email: string; createdAt: string };
export type AuthStatus = { needsSetup: boolean; user: UserView | null };
```
(Pastikan re-export via `shared/src/index.ts` bila file itu barrel — cek & samakan dgn `zSetting`.)

- [x] **Step 3:** `pnpm --filter ./shared typecheck` (atau `-r typecheck`) → PASS.
- [x] **Step 4: Commit** `git add shared/src && git commit -m "feat(shared): kontrak auth — paths + zod dto (SPEC-169)"`

---

### Task 4: Auth routes + enforcement hook

**Files:**
- Create: `server/src/routes/auth.ts`
- Modify: `server/src/app.ts` (register `@fastify/cookie`, register `auth`, tambah `onRequest` gate)
- Modify: `server/package.json` (dep `@fastify/cookie`)
- Create: `server/test/auth-routes.test.ts`

**Interfaces:**
- Consumes: `services/auth` (Task 2), `zLogin/zSignup/zChangePassword` (Task 3), `prisma`.
- Produces: endpoint tabel di spec. `req.user?: UserView` di-set oleh hook.
- Cookie diset via `reply.setCookie(COOKIE_NAME, token, cookieOpts())`; clear via `reply.clearCookie(COOKIE_NAME, { path: "/" })`.

- [x] **Step 1:** Install dep: `pnpm --filter ./server add @fastify/cookie`. Tambahkan ke daftar `--external:@fastify/cookie` di script `build` `server/package.json` (samakan pola `@fastify/static`).

- [x] **Step 2: Test dulu** — `server/test/auth-routes.test.ts` (pola: ikuti test route existing lain, mis. `test/*settings* / *specs*`; pakai `buildApp()` + `app.inject`). Alur:

```ts
// pseudo — sesuaikan helper reset-db seperti test server lain
import { describe, it, expect, beforeEach } from "vitest";
import { buildApp } from "../src/app";
import { prisma } from "../src/db";

const app = buildApp();
beforeEach(async () => { await prisma.session.deleteMany(); await prisma.user.deleteMany(); });

const cookieOf = (r: any) => (r.headers["set-cookie"] as string).split(";")[0];

it("full auth flow", async () => {
  // status → needsSetup
  let r = await app.inject({ method: "GET", url: "/api/auth/status" });
  expect(r.json()).toMatchObject({ needsSetup: true, user: null });
  // protected route without session → 401
  expect((await app.inject({ method: "GET", url: "/api/projects" })).statusCode).toBe(401);
  // setup first user
  r = await app.inject({ method: "POST", url: "/api/auth/setup", payload: { email: "a@b.co", password: "password1" } });
  expect(r.statusCode).toBe(200);
  const c1 = cookieOf(r);
  // setup again → 409
  expect((await app.inject({ method: "POST", url: "/api/auth/setup", payload: { email: "x@y.co", password: "password1" } })).statusCode).toBe(409);
  // protected route with cookie → 200
  expect((await app.inject({ method: "GET", url: "/api/projects", headers: { cookie: c1 } })).statusCode).toBe(200);
  // login wrong → 401, right → 200 + cookie
  expect((await app.inject({ method: "POST", url: "/api/auth/login", payload: { email: "a@b.co", password: "nope" } })).statusCode).toBe(401);
  r = await app.inject({ method: "POST", url: "/api/auth/login", payload: { email: "a@b.co", password: "password1" } });
  expect(r.statusCode).toBe(200);
  // invite user
  r = await app.inject({ method: "POST", url: "/api/auth/users", headers: { cookie: c1 }, payload: { email: "c@d.co", password: "password2" } });
  expect(r.statusCode).toBe(200);
  expect(r.json()).not.toHaveProperty("passwordHash");
  // list users → 2
  expect((await app.inject({ method: "GET", url: "/api/auth/users", headers: { cookie: c1 } })).json()).toHaveLength(2);
  // change password → old cookie invalid afterwards, new cookie works
  r = await app.inject({ method: "POST", url: "/api/auth/change-password", headers: { cookie: c1 }, payload: { currentPassword: "password1", newPassword: "password3" } });
  expect(r.statusCode).toBe(200);
  const c2 = cookieOf(r);
  expect((await app.inject({ method: "GET", url: "/api/projects", headers: { cookie: c1 } })).statusCode).toBe(401);
  expect((await app.inject({ method: "GET", url: "/api/projects", headers: { cookie: c2 } })).statusCode).toBe(200);
  // delete invited user, cannot delete last
  const others = (await app.inject({ method: "GET", url: "/api/auth/users", headers: { cookie: c2 } })).json() as any[];
  const other = others.find((u) => u.email === "c@d.co");
  expect((await app.inject({ method: "DELETE", url: `/api/auth/users/${other.id}`, headers: { cookie: c2 } })).statusCode).toBe(204);
  const me = others.find((u) => u.email === "a@b.co");
  expect((await app.inject({ method: "DELETE", url: `/api/auth/users/${me.id}`, headers: { cookie: c2 } })).statusCode).toBe(400); // last user
  // logout → cookie invalid
  await app.inject({ method: "POST", url: "/api/auth/logout", headers: { cookie: c2 } });
  expect((await app.inject({ method: "GET", url: "/api/projects", headers: { cookie: c2 } })).statusCode).toBe(401);
});
```

- [x] **Step 3:** Run → FAIL (route belum ada / 404 bukan 401).

- [x] **Step 4:** Implement `server/src/routes/auth.ts`:

```ts
import type { FastifyInstance, FastifyRequest, FastifyReply } from "fastify";
import { zLogin, zSignup, zChangePassword, type UserView } from "@hanoman/shared";
import { prisma } from "../db";
import * as auth from "../services/auth";

const view = (u: { id: string; email: string; createdAt: Date }): UserView =>
  ({ id: u.id, email: u.email, createdAt: u.createdAt.toISOString() });

async function issue(reply: FastifyReply, userId: string) {
  const token = await auth.createSession(userId);
  reply.setCookie(auth.COOKIE_NAME, token, auth.cookieOpts());
}

export default async function (app: FastifyInstance) {
  app.get("/auth/status", async (req: FastifyRequest) => {
    const count = await prisma.user.count();
    return { needsSetup: count === 0, user: req.user ?? null };
  });

  app.post("/auth/setup", async (req, reply) => {
    if (await prisma.user.count() > 0) return reply.code(409).send({ error: "already set up" });
    const p = zSignup.safeParse(req.body);
    if (!p.success) return reply.code(400).send({ error: p.error.flatten() });
    const user = await prisma.user.create({ data: { email: p.data.email, passwordHash: await auth.hashPassword(p.data.password) } });
    await issue(reply, user.id);
    return { user: view(user) };
  });

  app.post("/auth/login", async (req, reply) => {
    const ip = req.ip;
    if (auth.loginThrottle(ip).blocked) return reply.code(429).send({ error: "too many attempts" });
    const p = zLogin.safeParse(req.body);
    if (!p.success) return reply.code(400).send({ error: p.error.flatten() });
    const user = await prisma.user.findUnique({ where: { email: p.data.email } });
    if (!user || !(await auth.verifyPassword(p.data.password, user.passwordHash))) {
      auth.noteLoginFail(ip);
      return reply.code(401).send({ error: "email atau password salah" });
    }
    auth.clearLoginFails(ip);
    await issue(reply, user.id);
    return { user: view(user) };
  });

  app.post("/auth/logout", async (req, reply) => {
    const token = req.cookies?.[auth.COOKIE_NAME];
    if (token) await auth.deleteSession(token);
    reply.clearCookie(auth.COOKIE_NAME, { path: "/" });
    return reply.code(204).send();
  });

  app.get("/auth/users", async () => (await prisma.user.findMany({ orderBy: { createdAt: "asc" } })).map(view));

  app.post("/auth/users", async (req, reply) => {
    const p = zSignup.safeParse(req.body);
    if (!p.success) return reply.code(400).send({ error: p.error.flatten() });
    if (await prisma.user.findUnique({ where: { email: p.data.email } })) return reply.code(409).send({ error: "email dipakai" });
    const user = await prisma.user.create({ data: { email: p.data.email, passwordHash: await auth.hashPassword(p.data.password) } });
    return view(user);
  });

  app.delete<{ Params: { id: string } }>("/auth/users/:id", async (req, reply) => {
    if (await prisma.user.count() <= 1) return reply.code(400).send({ error: "tak bisa hapus user terakhir" });
    await prisma.user.deleteMany({ where: { id: req.params.id } });
    return reply.code(204).send();
  });

  app.post("/auth/change-password", async (req, reply) => {
    const p = zChangePassword.safeParse(req.body);
    if (!p.success) return reply.code(400).send({ error: p.error.flatten() });
    const me = req.user!;
    const user = await prisma.user.findUnique({ where: { id: me.id } });
    if (!user || !(await auth.verifyPassword(p.data.currentPassword, user.passwordHash))) return reply.code(400).send({ error: "password lama salah" });
    await prisma.user.update({ where: { id: user.id }, data: { passwordHash: await auth.hashPassword(p.data.newPassword) } });
    await auth.deleteUserSessions(user.id); // cabut semua sesi lama termasuk yang sekarang
    await issue(reply, user.id);            // re-issue sesi sekarang
    return { user: view(user) };
  });
}
```

- [x] **Step 5:** Wire di `server/src/app.ts`:
  - `import cookie from "@fastify/cookie";` dan `import authRoutes from "./routes/auth";` dan `import { COOKIE_NAME, lookupSession } from "./services/auth";`
  - Deklarasikan tipe: tambah `declare module "fastify" { interface FastifyRequest { user?: import("@hanoman/shared").UserView } }` (di app.ts atau file d.ts).
  - Di dalam `app.register(async (api) => { ... }, { prefix: "/api" })`, **paling atas** scope:

```ts
    await api.register(cookie);
    const PUBLIC = new Set(["GET /health", "GET /auth/status", "POST /auth/login", "POST /auth/setup"]);
    api.addHook("onRequest", async (req, reply) => {
      // routerPath relatif ke prefix scope → "/health", "/auth/login", dst.
      const rp = (req as any).routeOptions?.url ?? req.routerPath ?? "";
      if (PUBLIC.has(`${req.method} ${rp}`)) return;
      const token = req.cookies?.[COOKIE_NAME];
      const user = token ? await lookupSession(token) : null;
      if (!user) return reply.code(401).send({ error: "unauthorized" });
      req.user = user;
    });
    await api.register(authRoutes);
```
  (Lalu `await api.register(health/projects/...)` seperti sebelumnya.)
  Catatan verifikasi: `routerPath`/`routeOptions.url` harus cocok string di `PUBLIC`. Bila di runtime tak cocok (mis. berisi prefix), sesuaikan set-nya saat Step 6 — buktikan lewat test, bukan asumsi.

- [x] **Step 6:** Run test → PASS. Kalau gate salah men-cocokkan path, perbaiki matcher `rp` sampai hijau (jangan tebak — inspeksi nilai `req.routerPath` di runtime).
- [x] **Step 7:** Full server suite: `env -u NODE_ENV -u DATABASE_URL pnpm --filter ./server test`. Test route existing sekarang butuh sesi — **perbarui** helper test yang kena 401 (suntik cookie sesi, atau helper `authed()` yang setup+login sekali). Hijaukan semua.
- [x] **Step 8: Commit** `git add server && git commit -m "feat(server): route auth + gate 401 semua /api (SPEC-169)"`

---

### Task 5: Frontend — api client + gate App (Login/Setup)

**Files:**
- Modify: `src/src/api/client.ts`
- Create: `src/src/screens/AuthScreen.tsx`
- Modify: `src/src/App.tsx`

**Interfaces:**
- Consumes: `paths.auth*`, `AuthStatus`, `UserView` dari shared.
- Produces: `api.authStatus/login/logout/setup/listUsers/inviteUser/deleteUser/changePassword`.

- [ ] **Step 1:** `src/src/api/client.ts` — tambah import `AuthStatus, UserView` dan method:

```ts
  authStatus: () => j<AuthStatus>(paths.authStatus),
  setup: (b: { email: string; password: string }) => j<{ user: UserView }>(paths.authSetup, { method: "POST", ...body(b) }),
  login: (b: { email: string; password: string }) => j<{ user: UserView }>(paths.authLogin, { method: "POST", ...body(b) }),
  logout: () => j<void>(paths.authLogout, { method: "POST" }),
  listUsers: () => j<UserView[]>(paths.authUsers),
  inviteUser: (b: { email: string; password: string }) => j<UserView>(paths.authUsers, { method: "POST", ...body(b) }),
  deleteUser: (id: string) => j<void>(paths.authUser(id), { method: "DELETE" }),
  changePassword: (b: { currentPassword: string; newPassword: string }) =>
    j<{ user: UserView }>(paths.authChangePassword, { method: "POST", ...body(b) }),
```
(Cookie ikut otomatis — same-origin; `fetch` default `credentials:"same-origin"`. Tak perlu ubah `j`.)

- [ ] **Step 2:** `src/src/screens/AuthScreen.tsx` — satu komponen login+setup, pakai komponen `ds` (Input, Button, Field), gaya bone-paper. Props `{ needsSetup: boolean; onDone: (u: UserView) => void }`. Submit → `api.setup`/`api.login`; error → tampilkan "email atau password salah". Password `min 8` untuk setup. (Layar penuh, bukan Modal.)

- [ ] **Step 3:** `src/src/App.tsx` — sebelum render app:
  - state `auth: AuthStatus | null` (null=loading). `React.useEffect` on mount → `api.authStatus().then(setAuth)`.
  - Jika `auth===null` → splash `StateBlock loading`.
  - Jika `auth.needsSetup` → `<AuthScreen needsSetup onDone={(u)=>setAuth({needsSetup:false,user:u})} />`.
  - Jika `!auth.user` → `<AuthScreen needsSetup={false} onDone={(u)=>setAuth({needsSetup:false,user:u})} />`.
  - Else render app existing; teruskan `me={auth.user}` + `onLoggedOut={()=>setAuth({needsSetup:false,user:null})}` ke Settings.
  - Di `j()` global tak diubah; tapi tangani 401 dari `load()`/mutasi: bila `ApiError.status===401` → `setAuth({needsSetup:false,user:null})` (balik ke login). Tambah helper kecil `onAuthErr(e)` dipakai di `.catch`.

- [ ] **Step 4:** Manual smoke (Task 7 verifikasi penuh). Typecheck `pnpm --filter ./src typecheck` (atau `-r`). Jalankan test frontend bila ada yang kena.
- [ ] **Step 5: Commit** `git add src/src && git commit -m "feat(web): gate app di balik Login/Setup (SPEC-169)"`

---

### Task 6: Frontend — SettingsScreen panel Akun + Users

**Files:**
- Modify: `src/src/screens/SettingsScreen.tsx`
- Modify: `src/src/App.tsx` (teruskan `me` + `onLoggedOut` — sudah disiapkan Task 5)

**Interfaces:**
- Consumes: `api.changePassword/listUsers/inviteUser/deleteUser/logout`, prop `me: UserView`, `onLoggedOut()`.

- [ ] **Step 1:** SettingsScreen terima props baru `{ me: UserView; onLoggedOut: () => void }` (selain `onToast` existing).
- [ ] **Step 2:** Panel **Akun**: tampilkan `me.email`; tombol **Logout** (`api.logout().then(onLoggedOut)`); form **Ganti password** (currentPassword + newPassword min8 → `api.changePassword` → toast sukses; error 400 → toast "password lama salah").
- [ ] **Step 3:** Panel **Users**: `React.useEffect` load `api.listUsers()`; daftar email + createdAt; tombol hapus per baris (`api.deleteUser` → refresh; error 400 → toast "tak bisa hapus user terakhir"); form invite (email+password min8 → `api.inviteUser` → refresh; error 409 → toast "email dipakai").
- [ ] **Step 4:** Typecheck `-r` → PASS.
- [ ] **Step 5: Commit** `git add src/src && git commit -m "feat(web): Settings — akun, ganti password, kelola users (SPEC-169)"`

---

### Task 7: Docs (Source of Truth) + server comment + ADR + verifikasi nyata

**Files:**
- Modify: `server/src/server.ts` (komentar)
- Create: `internal/docs/adr/0028-auth-sesi-opaque-di-db.md`
- Modify: `internal/docs/security/security-standard.md`
- Modify: `internal/docs/architecture/api-contract.md`
- Modify: `internal/docs/architecture/data-model.md`

- [ ] **Step 1:** `server.ts`: perbarui komentar `host` — auth kini ada; default tetap `127.0.0.1` di belakang reverse proxy TLS; `HOST=0.0.0.0` hanya bila ada proxy TLS di depan.
- [ ] **Step 2:** ADR-0028 (context/decision/consequences): sesi opaque revocable di DB (bukan JWT), hash scrypt stdlib, gate onRequest, bind 127.0.0.1 + reverse proxy TLS, tanpa RBAC. Rujuk komentar `server.ts` lama & SPEC-169.
- [ ] **Step 3:** `security-standard.md`: tambah butir **Auth** (login email/password, hash scrypt+salt+timingSafeEqual, sesi opaque `sha256(token)` revocable, cookie httpOnly/Secure/SameSite=strict, throttle login, error generic) + **TLS**: contoh Caddyfile (`reverse_proxy 127.0.0.1:8787`, auto-HTTPS).
- [ ] **Step 4:** `api-contract.md`: tambah bagian `/api/auth/**` (tabel endpoint) + catatan semua route lain 401 tanpa sesi. `data-model.md`: tambah `User`/`Session`.
- [ ] **Step 5: Verifikasi nyata (WAJIB — bukan hanya unit test).** Boot server di port non-dev (lihat memory: 8787 dipakai sesi dev lain), `curl` alur:
  ```
  status(needsSetup:true) → 401 pada /api/projects → setup (simpan cookie)
  → status(user) → /api/projects 200 dgn cookie → login → invite → list(2)
  → change-password (cookie lama 401, cookie baru 200) → delete user → logout → 401
  ```
  Gunakan `curl -c/-b cookiejar`. Semua langkah harus sesuai harapan; kalau ada yang gagal, fix sampai hijau.
- [ ] **Step 6:** Full suite: `env -u NODE_ENV -u DATABASE_URL pnpm test` → hijau.
- [ ] **Step 7: Commit** `git add internal/docs server/src/server.ts && git commit -m "docs(spec-169): ADR-0028 + security/architecture auth; server comment"`

---

## Self-Review

- **Spec coverage:** login (T4/T5), invite set-password-langsung (T4 `/auth/users` + T6), ganti password (T4 + T6), gate 401 (T4), bootstrap setup layar-pertama (T4 `/auth/setup` + T5), sesi revocable (T2/T4), scrypt (T2), TLS/reverse-proxy (T7), list+delete user (T4/T6), docs+ADR (T7). ✔
- **Placeholder scan:** tak ada TBD/TODO; kode inti (service, routes, hook) lengkap. Frontend T5/T6 dideskripsikan per-langkah dgn API eksak (komponen UI mengikuti pola `ds`/screens existing — sengaja tak menyalin 200 baris JSX; interface & pemanggilan API eksak).
- **Type consistency:** `UserView`/`AuthStatus` (T3) dipakai konsisten di service-return, routes `view()`, client, screens. `COOKIE_NAME`/`cookieOpts`/`createSession`→token dipakai konsisten T2↔T4. `sessionId`=sha256 konsisten.
- **Risiko diketahui:** matcher path di gate hook (`req.routerPath`) diverifikasi runtime di T4 Step 6 (bukan asumsi); test route existing perlu di-update untuk kirim cookie (T4 Step 7).
