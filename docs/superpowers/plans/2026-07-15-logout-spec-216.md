# Logout (discoverable) — SPEC-216 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Hadirkan afordans logout yang langsung terlihat di chrome utama (topbar), sehingga pengguna bisa keluar dari layar mana pun — logout server sudah ada & teruji.

**Architecture:** Widget `AccountMenu` baru di topbar `Shell`, mengonsumsi `AuthContext` (provider di `App`). Mirror pola `NotificationBell` → nol prop-threading ke ~9 call-site `<Shell>`, nilai default context aman untuk test tanpa provider. Tak ada perubahan server/skema/kontrak API.

**Tech Stack:** React 18 + TypeScript (Vite), vitest + @testing-library/react. DS primitif diimpor langsung dari file komponen (bukan barrel `../ds`) untuk menghindari siklus impor via `Shell`.

## Global Constraints
- TypeScript strict; tak ada perubahan server, skema Prisma, atau kontrak API — `POST /auth/logout` sudah ada (ADR-0028) dan teruji.
- Impor DS di widget topbar dari file langsung (`../ds/icon`), BUKAN dari barrel `../ds` (barrel mengekspor `Shell` → siklus).
- Prosa/label UI bahasa Indonesia; tombol logout berlabel **"Keluar"**.
- Tombol logout lama di Settings → Akun **tetap ada** (jangan dihapus/di-refactor besar).
- Update `internal/docs` yang tersentuh dalam commit yang sama.

---

### Task 1: AuthContext (provider + hook + default aman)

**Files:**
- Create: `src/src/auth/AuthContext.tsx`
- Test: `src/test/auth-context.test.tsx`

**Interfaces:**
- Consumes: `api.logout()` dari `src/src/api/client.ts`; `UserView` dari `@hanoman/shared`.
- Produces:
  - `AuthContext: React.Context<{ user: UserView | null; logout: () => Promise<void> }>` (default `{ user: null, logout: async () => {} }`)
  - `useAuth(): { user: UserView | null; logout: () => Promise<void> }`
  - `AuthProvider(props: { user: UserView; onLoggedOut: () => void; children: React.ReactNode })`

- [ ] **Step 1: Tulis test yang gagal**

Create `src/test/auth-context.test.tsx`:

```tsx
import { describe, it, expect, vi, beforeEach } from "vitest";
import { render, screen, fireEvent, waitFor } from "@testing-library/react";
import { AuthProvider, useAuth } from "../src/auth/AuthContext";
import { api } from "../src/api/client";

vi.mock("../src/api/client", () => ({ api: { logout: vi.fn() } }));

function Probe() {
  const { user, logout } = useAuth();
  return <button onClick={() => void logout()}>{user ? user.email : "no-user"}</button>;
}
const me = { id: "u1", email: "a@b.c", createdAt: new Date().toISOString() };

beforeEach(() => { (api.logout as any).mockReset(); });

describe("AuthContext", () => {
  it("default aman: user null tanpa provider", () => {
    render(<Probe />);
    expect(screen.getByText("no-user")).toBeInTheDocument();
  });

  it("logout memanggil api.logout lalu onLoggedOut", async () => {
    (api.logout as any).mockResolvedValue(undefined);
    const onLoggedOut = vi.fn();
    render(<AuthProvider user={me} onLoggedOut={onLoggedOut}><Probe /></AuthProvider>);
    fireEvent.click(screen.getByText("a@b.c"));
    await waitFor(() => expect(api.logout).toHaveBeenCalled());
    await waitFor(() => expect(onLoggedOut).toHaveBeenCalled());
  });

  it("tetap membersihkan klien meski api.logout gagal", async () => {
    (api.logout as any).mockRejectedValue(new Error("net"));
    const onLoggedOut = vi.fn();
    render(<AuthProvider user={me} onLoggedOut={onLoggedOut}><Probe /></AuthProvider>);
    fireEvent.click(screen.getByText("a@b.c"));
    await waitFor(() => expect(onLoggedOut).toHaveBeenCalled());
  });
});
```

- [ ] **Step 2: Jalankan test — pastikan GAGAL**

Run: `cd src && pnpm vitest run test/auth-context.test.tsx`
Expected: FAIL — modul `../src/auth/AuthContext` belum ada.

- [ ] **Step 3: Implementasi minimal**

Create `src/src/auth/AuthContext.tsx`:

```tsx
import React from "react";
import type { UserView } from "@hanoman/shared";
import { api } from "../api/client";

type Ctx = { user: UserView | null; logout: () => Promise<void> };

// Nilai default aman: <Shell>/<AccountMenu> yang dirender tanpa provider (mis. test)
// tak error dan AccountMenu memilih tak menampilkan apa-apa (user null).
export const AuthContext = React.createContext<Ctx>({ user: null, logout: async () => {} });
export const useAuth = () => React.useContext(AuthContext);

export function AuthProvider({ user, onLoggedOut, children }:
  { user: UserView; onLoggedOut: () => void; children: React.ReactNode }) {
  // Bersihkan state klien di `finally`: walau panggilan jaringan gagal, pengguna tetap
  // dikembalikan ke Login — konsisten dengan AccountPanel (SettingsScreen).
  const logout = React.useCallback(async () => {
    try { await api.logout(); } finally { onLoggedOut(); }
  }, [onLoggedOut]);
  return <AuthContext.Provider value={{ user, logout }}>{children}</AuthContext.Provider>;
}
```

- [ ] **Step 4: Jalankan test — pastikan LULUS**

Run: `cd src && pnpm vitest run test/auth-context.test.tsx`
Expected: PASS (3 test).

- [ ] **Step 5: Commit**

```bash
git add src/src/auth/AuthContext.tsx src/test/auth-context.test.tsx
git commit -m "feat(web): AuthContext — user + logout via context (SPEC-216)"
```

---

### Task 2: AccountMenu (widget topbar)

**Files:**
- Create: `src/src/auth/AccountMenu.tsx`
- Test: `src/test/account-menu.test.tsx`

**Interfaces:**
- Consumes: `useAuth()` + `AuthContext` dari Task 1; `Icon` dari `../ds/icon`.
- Produces: `AccountMenu()` — komponen tanpa prop. `null` bila `user` null.

- [ ] **Step 1: Tulis test yang gagal**

Create `src/test/account-menu.test.tsx`:

```tsx
import { describe, it, expect, vi } from "vitest";
import { render, screen, fireEvent } from "@testing-library/react";
import { AccountMenu } from "../src/auth/AccountMenu";
import { AuthContext } from "../src/auth/AuthContext";

function Harness({ user, logout }: { user: any; logout?: () => Promise<void> }) {
  return (
    <AuthContext.Provider value={{ user, logout: logout ?? (async () => {}) }}>
      <AccountMenu />
    </AuthContext.Provider>
  );
}
const me = { id: "u1", email: "dena@nafanesia.id", createdAt: "x" };

describe("AccountMenu", () => {
  it("tak merender apa-apa tanpa user (default aman)", () => {
    const { container } = render(<AccountMenu />);
    expect(container.firstChild).toBeNull();
  });

  it("menampilkan inisial email di tombol akun", () => {
    render(<Harness user={me} />);
    expect(screen.getByLabelText("Akun")).toBeInTheDocument();
    expect(screen.getByText("D")).toBeInTheDocument();
  });

  it("klik akun → popover email; Keluar memanggil logout", () => {
    const logout = vi.fn().mockResolvedValue(undefined);
    render(<Harness user={me} logout={logout} />);
    // popover tertutup: email belum tampil
    expect(screen.queryByText("dena@nafanesia.id")).toBeNull();
    fireEvent.click(screen.getByLabelText("Akun"));
    expect(screen.getByText("dena@nafanesia.id")).toBeInTheDocument();
    fireEvent.click(screen.getByText("Keluar"));
    expect(logout).toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: Jalankan test — pastikan GAGAL**

Run: `cd src && pnpm vitest run test/account-menu.test.tsx`
Expected: FAIL — modul `../src/auth/AccountMenu` belum ada.

- [ ] **Step 3: Implementasi minimal**

Create `src/src/auth/AccountMenu.tsx`:

```tsx
import React from "react";
import { Icon } from "../ds/icon";
import { useAuth } from "./AuthContext";

// Widget topbar akun — mirror NotificationBell: konsumsi context, klik-luar menutup.
// user null (default context / belum login) → tak merender apa-apa.
export function AccountMenu() {
  const { user, logout } = useAuth();
  const [open, setOpen] = React.useState(false);
  const ref = React.useRef<HTMLDivElement>(null);

  React.useEffect(() => {
    if (!open) return;
    const onDoc = (e: MouseEvent) => { if (ref.current && !ref.current.contains(e.target as Node)) setOpen(false); };
    document.addEventListener("mousedown", onDoc);
    return () => document.removeEventListener("mousedown", onDoc);
  }, [open]);

  if (!user) return null;
  const initial = user.email.slice(0, 1).toUpperCase();

  return (
    <div ref={ref} style={{ position: "relative" }}>
      <button aria-label="Akun" title={user.email} onClick={() => setOpen((v) => !v)} style={{
        display: "inline-flex", alignItems: "center", justifyContent: "center", width: 34, height: 34,
        border: "none", background: open ? "var(--bone-200)" : "transparent",
        borderRadius: "var(--radius-sm)", cursor: "pointer",
      }}>
        <span style={{
          display: "inline-flex", alignItems: "center", justifyContent: "center", width: 24, height: 24,
          borderRadius: "50%", background: "var(--brass-100)", color: "var(--brass-700)",
          fontFamily: "var(--font-mono)", fontSize: 12, fontWeight: 600, lineHeight: 1,
        }}>{initial}</span>
      </button>
      {open && (
        <div role="menu" style={{
          position: "absolute", top: 40, right: 0, width: 220, background: "var(--surface-card)",
          border: "1px solid var(--border-hair)", borderRadius: "var(--radius-md)",
          boxShadow: "var(--shadow-xl)", zIndex: 200, padding: 6,
        }}>
          <div style={{ padding: "8px 10px 6px" }}>
            <div style={{ fontSize: 10.5, textTransform: "uppercase", letterSpacing: "0.06em", color: "var(--text-subtle)", marginBottom: 3 }}>
              Masuk sebagai
            </div>
            <div style={{ fontSize: 13, color: "var(--text-strong)", wordBreak: "break-all" }}>{user.email}</div>
          </div>
          <button onClick={() => { setOpen(false); void logout(); }} style={{
            display: "flex", alignItems: "center", gap: 8, width: "100%", marginTop: 4, padding: "9px 10px",
            border: "none", borderTop: "1px solid var(--border-hair)", background: "transparent",
            cursor: "pointer", color: "var(--text-body)", fontSize: 13, fontFamily: "var(--font-ui)", textAlign: "left",
          }}>
            <Icon name="log-out" size={16} color="var(--text-muted)" /> Keluar
          </button>
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 4: Jalankan test — pastikan LULUS**

Run: `cd src && pnpm vitest run test/account-menu.test.tsx`
Expected: PASS (3 test).

- [ ] **Step 5: Commit**

```bash
git add src/src/auth/AccountMenu.tsx src/test/account-menu.test.tsx
git commit -m "feat(web): AccountMenu — identitas + Keluar di topbar (SPEC-216)"
```

---

### Task 3: Wire AccountMenu ke Shell + AuthProvider ke App

**Files:**
- Modify: `src/src/ds/shell.tsx` (import + render `<AccountMenu />` di topbar)
- Modify: `src/src/App.tsx` (bungkus render dengan `<AuthProvider>`)
- Test: `src/test/logout.test.tsx` (end-to-end via `<App />`)

**Interfaces:**
- Consumes: `AccountMenu` (Task 2), `AuthProvider` (Task 1). App sudah punya `me: UserView` dan `onLoggedOut` (`src/src/App.tsx:298,517`).
- Produces: afordans logout terlihat di semua layar ter-autentikasi.

- [ ] **Step 1: Tulis test end-to-end yang gagal**

Create `src/test/logout.test.tsx`:

```tsx
import { render, screen, fireEvent, waitFor } from "@testing-library/react";
import { describe, it, expect, vi } from "vitest";

const logout = vi.fn(async () => {});
vi.mock("../src/api/client", () => ({
  api: {
    authStatus: vi.fn(async () => ({ needsSetup: false, user: { id: "u1", email: "a@b.co", createdAt: "" } })),
    listProjects: vi.fn(async () => ({ items: [], total: 0, page: 1, pageSize: 20 })),
    listSpecs: vi.fn(async () => ({ items: [], total: 0, page: 1, pageSize: 20 })),
    listTerminals: vi.fn(async () => []),
    getSettings: vi.fn(async () => ({})),
    listNotifications: vi.fn(async () => ({ items: [], unread: 0 })),
    logout,
  },
  ApiError: class extends Error {},
}));
import App from "../src/App";

describe("logout dari topbar", () => {
  it("tombol Akun → Keluar memanggil api.logout dan kembali ke Login", async () => {
    render(<App />);
    // sudah login: menu Akun muncul di topbar
    const acct = await screen.findByLabelText("Akun");
    fireEvent.click(acct);
    expect(screen.getByText("a@b.co")).toBeInTheDocument();
    fireEvent.click(screen.getByText("Keluar"));
    await waitFor(() => expect(logout).toHaveBeenCalled());
    // kembali ke AuthScreen
    await waitFor(() => expect(screen.getByText("Masuk")).toBeInTheDocument());
  });
});
```

- [ ] **Step 2: Jalankan test — pastikan GAGAL**

Run: `cd src && pnpm vitest run test/logout.test.tsx`
Expected: FAIL — `getByLabelText("Akun")` tak ditemukan (belum di-wire).

- [ ] **Step 3a: Render AccountMenu di Shell topbar**

In `src/src/ds/shell.tsx`, tambah import setelah baris `import { UpdateBadge } ...` (baris 9):

```tsx
import { AccountMenu } from "../auth/AccountMenu";
```

Lalu di topbar, setelah `{actions}` (baris 134), tambahkan `<AccountMenu />` sebagai anchor kanan-jauh:

```tsx
          {actions}
          <AccountMenu />
```

- [ ] **Step 3b: Bungkus App dengan AuthProvider**

In `src/src/App.tsx`, tambah import setelah `import { AuthScreen } ...` (baris 11):

```tsx
import { AuthProvider } from "./auth/AuthContext";
```

Ubah blok `return (` terakhir (baris 648-658) dari:

```tsx
  return (
    <NotificationsProvider showToast={showToast} onOpen={openNotification}>
      {screen}
      <NewSpecModal open={modal === "brief"} onClose={() => { setModal(null); setSpecPrefill(null); }}
        projects={projectsView} defaultProject={proj ? proj.id : ""} onCreate={createSpec}
        prefill={specPrefill ?? undefined} />
      <NewProjectModal open={modal === "project"} onClose={() => setModal(null)} onCreate={createProject} />
      <EditProjectModal open={modal === "project-edit"} project={proj} onClose={() => setModal(null)} onSave={updateProject} />
      <Toast toast={toast} />
    </NotificationsProvider>
  );
```

menjadi (bungkus dengan `<AuthProvider user={me} onLoggedOut={onLoggedOut}>`):

```tsx
  return (
    <AuthProvider user={me} onLoggedOut={onLoggedOut}>
      <NotificationsProvider showToast={showToast} onOpen={openNotification}>
        {screen}
        <NewSpecModal open={modal === "brief"} onClose={() => { setModal(null); setSpecPrefill(null); }}
          projects={projectsView} defaultProject={proj ? proj.id : ""} onCreate={createSpec}
          prefill={specPrefill ?? undefined} />
        <NewProjectModal open={modal === "project"} onClose={() => setModal(null)} onCreate={createProject} />
        <EditProjectModal open={modal === "project-edit"} project={proj} onClose={() => setModal(null)} onSave={updateProject} />
        <Toast toast={toast} />
      </NotificationsProvider>
    </AuthProvider>
  );
```

- [ ] **Step 4: Jalankan test — pastikan LULUS**

Run: `cd src && pnpm vitest run test/logout.test.tsx`
Expected: PASS.

- [ ] **Step 5: Regresi — seluruh test frontend hijau**

Run: `cd src && pnpm vitest run`
Expected: PASS (semua, termasuk app-flows/app-states/settings-nav yang me-render App/Shell).

- [ ] **Step 6: Commit**

```bash
git add src/src/ds/shell.tsx src/src/App.tsx src/test/logout.test.tsx
git commit -m "feat(web): logout terlihat di topbar (AccountMenu + AuthProvider) — SPEC-216"
```

---

### Task 4: Update SoT docs + verifikasi akhir

**Files:**
- Modify: `internal/docs/frontend/frontend-implementation.md`
- Verify: build frontend + boot app + smoke logout end-to-end

- [ ] **Step 1: Dokumentasikan AccountMenu di SoT**

In `internal/docs/frontend/frontend-implementation.md`, di area daftar widget topbar (dekat deskripsi `NotificationBell`/`UpdateBadge`, sekitar baris 252-257), tambahkan butir:

```markdown
- **`AccountMenu`** (`src/auth/AccountMenu.tsx`) — widget topbar `Shell` paling kanan: tombol
  avatar (inisial email) yang membuka popover berisi email + tombol **Keluar** (SPEC-216).
  Mengonsumsi `AuthContext` (`src/auth/AuthContext.tsx`, provider di `App`) — nol prop-threading
  ke ~9 call-site `<Shell>`; nilai default context aman (user null → tak merender apa-apa) untuk
  test tanpa provider. `Keluar` memanggil `POST /auth/logout` lalu balik ke Login (`finally` tetap
  membersihkan klien bila jaringan gagal). Tombol logout sekunder tetap ada di Settings → Akun.
```

Bila baris 5 (daftar "Bagian: … Settings (…)") menyebut afordans akun, tak perlu diubah — logout kini juga di topbar, sudah dijelaskan butir di atas.

- [ ] **Step 2: Build frontend (typecheck + bundle)**

Run: `cd src && pnpm build`
Expected: sukses tanpa error TypeScript (membuktikan wiring Shell/App benar).

- [ ] **Step 3: Full test suite (frontend) hijau**

Run: `cd src && pnpm vitest run`
Expected: PASS.

- [ ] **Step 4: Smoke logout end-to-end nyata**

Verifikasi jalur nyata `POST /auth/logout` (endpoint tak berubah, tapi buktikan tetap hijau):

```bash
cd server && env -u NODE_ENV -u DATABASE_URL pnpm vitest run test/auth-routes.test.ts test/parity-endpoints.test.ts
```
Expected: PASS — full flow (setup → login → … → logout 204) tetap hijau.

Bila memungkinkan, boot app dan cek UI: tombol avatar muncul di topbar, klik → popover email + Keluar, klik Keluar → kembali ke layar Masuk. (App tanpa routing URL — smoke lewat build preview / CDP sesuai kebiasaan repo.)

- [ ] **Step 5: Commit**

```bash
git add internal/docs/frontend/frontend-implementation.md
git commit -m "docs(frontend): AccountMenu topbar — logout terlihat (SPEC-216)"
```

---

## Self-Review

**Spec coverage:**
- Objective "dapat melakukan logout" dari mana pun → Task 2 (AccountMenu) + Task 3 (wire ke semua Shell via topbar). ✅
- Pola context nol prop-threading + default aman → Task 1. ✅
- Tak ada perubahan server/API → hanya Task 4 memverifikasi endpoint existing. ✅
- Docs tersentuh diperbarui commit yang sama → Task 4. ✅
- Tombol Settings→Akun tetap ada → tak ada task yang menghapusnya. ✅

**Placeholder scan:** Tak ada TBD/TODO; tiap step berisi kode/perintah konkret. ✅

**Type consistency:** `AuthContext`/`useAuth`/`AuthProvider` (Task 1) dipakai konsisten di `AccountMenu` (Task 2) dan `App`/`Shell` (Task 3); `AccountMenu()` tanpa prop di semua tempat; `UserView` dari `@hanoman/shared`. ✅
