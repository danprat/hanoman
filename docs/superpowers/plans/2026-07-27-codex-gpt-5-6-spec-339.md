# SPEC-339 — Katalog Codex GPT-5.6 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [x]`) syntax for tracking.

**Goal:** Sesi codex bisa memakai `gpt-5.6-sol` / `gpt-5.6-terra` / `gpt-5.6-luna` berikut effort `max` & `ultra`, tanpa pernah melahirkan kombinasi model×effort yang ditolak Codex CLI.

**Architecture:** Katalog codex di `shared` berubah dari dua daftar sejajar (`CODEX_MODELS` + `CODEX_EFFORTS`) menjadi satu daftar model yang membawa daftar effort-nya sendiri, ditemani dua fungsi murni (`codexEfforts`, `coerceCodexEffort`). Server memakai fungsi itu di dua tempat: `getSetting()` (meremap model pensiun + mengoreksi effort tersimpan) dan `createSession()` (titik cekik tunggal seluruh kelahiran sesi). UI membaca daftar effort dari model terpilih, dan sebuah endpoint versi baru memberi peringatan lunak bila codex CLI terlalu tua.

**Tech Stack:** TypeScript strict, zod (shared), Fastify + Prisma (server), React + Vite + Testing Library (src), vitest di semua paket.

Spec: `docs/superpowers/specs/2026-07-27-spec-339-codex-gpt-5-6-design.md`

## Global Constraints

- **Tanpa migration, tanpa ADR baru.** `Setting` tetap kolom `Json`; ini perluasan ADR-0074.
- **Nilai katalog verbatim dari manifest codex-cli 0.145.0** — jangan mengarang slug atau effort:
  - `gpt-5.6-sol` — efforts `ultra, max, xhigh, high, medium, low` — `minClient` `0.144.0`
  - `gpt-5.6-terra` — efforts `ultra, max, xhigh, high, medium, low` — `minClient` `0.144.0`
  - `gpt-5.6-luna` — efforts `max, xhigh, high, medium, low` (**tanpa `ultra`**) — `minClient` `0.144.0`
  - `gpt-5.5` — efforts `xhigh, high, medium, low` — `minClient` `0.124.0`
- **`fallback` semua model = `xhigh`.** Default codex global = `gpt-5.6-sol` + `xhigh`.
- **Peta pensiun → `gpt-5.5`** (bukan 5.6): `gpt-5.4`, `gpt-5.4-mini`, `gpt-5.3-codex-spark`.
- **Katalog claude tak boleh tersentuh** — `MODELS`, `EFFORTS`, `RETIRED_MODELS`, `Setting.model`, `Setting.effort`.
- **Peringatan versi tak pernah memblokir Start** (ADR-0037: agen dipercaya, isolasi = worktree).
- **Prosa kode & komentar berbahasa Indonesia**, mengikuti gaya berkas sekitarnya.
- Test server memakai DB khusus SPEC ini: base `hanoman339` (vitest memakai `hanoman339_test`), selalu `--no-file-parallelism`, dan selalu `env -u NODE_ENV -u DATABASE_URL` bila menjalankan seluruh suite (shell dev menunjuk prod dan membuat ~41 test gagal palsu).
- Repo ini dipakai bersama sesi lain: **`git add <path eksplisit>` saja**, jangan `git add -A`, jangan `git stash`.

---

### Task 0: Worktree & DB test

**Files:** tak ada perubahan berkas.

- [x] **Step 1: Buat worktree terisolasi**

```bash
git worktree add .worktrees/spec-339 -b hanoman/spec-339 main
cd .worktrees/spec-339
pnpm install
pnpm --filter ./server exec prisma generate
```

- [x] **Step 2: Siapkan DB test khusus SPEC-339**

```bash
docker exec hanoman-db-1 psql -U hanoman -d postgres -c 'CREATE DATABASE hanoman339_test'
DATABASE_URL=postgresql://hanoman:hanoman@localhost:5432/hanoman339_test \
  pnpm --filter ./server exec prisma migrate deploy
```

Expected: `All migrations have been successfully applied` (atau `No pending migrations`).

- [x] **Step 3: Pastikan baseline hijau sebelum menyentuh apa pun**

Run: `env -u NODE_ENV -u DATABASE_URL pnpm test`
Expected: seluruh suite PASS. Bila ada yang merah **sebelum** perubahan, catat dan laporkan — jangan lanjut menumpuk perubahan di atas suite merah.

---

### Task 1: Katalog per-model di `shared`

**Files:**
- Modify: `shared/src/entities.ts:54-72`
- Create: `shared/src/codex-catalog.test.ts`
- Modify: `shared/src/agent-session.test.ts:12-20`

**Interfaces:**
- Produces: `type CodexModel = { id: string; label: string; efforts: readonly string[]; fallback: string; minClient: string }`; `CODEX_MODELS: readonly CodexModel[]`; `CODEX_EFFORTS: readonly string[]` (gabungan); `codexModel(id: string): CodexModel | undefined`; `codexEfforts(modelId: string): readonly string[]`; `coerceCodexEffort(modelId: string, effort: string): string`; `RETIRED_CODEX_MODELS: Record<string, string>`; `CODEX_DEFAULTS: Codex` (kini `{ model: "gpt-5.6-sol", effort: "xhigh" }`).

- [x] **Step 1: Tulis test yang gagal**

Buat `shared/src/codex-catalog.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import {
  CODEX_MODELS, CODEX_EFFORTS, CODEX_DEFAULTS, RETIRED_CODEX_MODELS,
  codexModel, codexEfforts, coerceCodexEffort,
} from "./entities";

describe("SPEC-339 · katalog codex per-model", () => {
  it("memuat trio GPT-5.6 + gpt-5.5, tanpa model yang dipensiunkan", () => {
    expect(CODEX_MODELS.map((m) => m.id)).toEqual([
      "gpt-5.6-sol", "gpt-5.6-terra", "gpt-5.6-luna", "gpt-5.5",
    ]);
  });

  // Nilai verbatim dari `codex debug models` (codex-cli 0.145.0). Luna TIDAK mendukung ultra.
  it("Luna tak mendukung ultra; 5.5 tak mendukung max maupun ultra", () => {
    expect(codexEfforts("gpt-5.6-sol")).toEqual(["ultra", "max", "xhigh", "high", "medium", "low"]);
    expect(codexEfforts("gpt-5.6-luna")).toEqual(["max", "xhigh", "high", "medium", "low"]);
    expect(codexEfforts("gpt-5.5")).toEqual(["xhigh", "high", "medium", "low"]);
  });

  it("minClient mengikuti manifest: trio 5.6 butuh 0.144.0", () => {
    expect(codexModel("gpt-5.6-sol")?.minClient).toBe("0.144.0");
    expect(codexModel("gpt-5.6-terra")?.minClient).toBe("0.144.0");
    expect(codexModel("gpt-5.6-luna")?.minClient).toBe("0.144.0");
    expect(codexModel("gpt-5.5")?.minClient).toBe("0.124.0");
  });

  it("effort tak didukung diturunkan ke fallback model", () => {
    expect(coerceCodexEffort("gpt-5.6-luna", "ultra")).toBe("xhigh");
    expect(coerceCodexEffort("gpt-5.5", "max")).toBe("xhigh");
    expect(coerceCodexEffort("gpt-5.5", "ultra")).toBe("xhigh");
  });

  it("effort yang didukung diteruskan apa adanya", () => {
    expect(coerceCodexEffort("gpt-5.6-sol", "ultra")).toBe("ultra");
    expect(coerceCodexEffort("gpt-5.6-luna", "max")).toBe("max");
    expect(coerceCodexEffort("gpt-5.5", "low")).toBe("low");
  });

  // Katalog kita TIDAK boleh jadi gerbang yang memblokir model yang belum sempat didaftar —
  // server sengaja lenient (z.string()).
  it("model tak dikenal: effort diteruskan apa adanya, daftar jatuh ke irisan aman", () => {
    expect(coerceCodexEffort("gpt-7-belum-ada", "ultra")).toBe("ultra");
    expect(codexEfforts("gpt-7-belum-ada")).toEqual(["xhigh", "high", "medium", "low"]);
    expect(codexModel("gpt-7-belum-ada")).toBeUndefined();
  });

  it("model pensiun dipetakan ke gpt-5.5 — aman untuk CLI lama", () => {
    expect(RETIRED_CODEX_MODELS["gpt-5.3-codex-spark"]).toBe("gpt-5.5");
    expect(RETIRED_CODEX_MODELS["gpt-5.4"]).toBe("gpt-5.5");
    expect(RETIRED_CODEX_MODELS["gpt-5.4-mini"]).toBe("gpt-5.5");
  });

  it("default codex = gpt-5.6-sol / xhigh, dan CODEX_EFFORTS adalah gabungan", () => {
    expect(CODEX_DEFAULTS).toEqual({ model: "gpt-5.6-sol", effort: "xhigh" });
    expect(CODEX_EFFORTS).toEqual(["ultra", "max", "xhigh", "high", "medium", "low"]);
  });
});
```

- [x] **Step 2: Jalankan test, pastikan GAGAL**

Run: `env -u NODE_ENV DATABASE_URL=postgresql://hanoman:hanoman@localhost:5432/hanoman339 pnpm vitest run --no-file-parallelism shared/src/codex-catalog.test.ts`
Expected: FAIL — `codexEfforts is not a function` / `coerceCodexEffort is not exported`.

- [x] **Step 3: Ganti blok katalog codex di `shared/src/entities.ts`**

Ganti baris 54–72 (blok `CODEX_MODELS` / `CODEX_EFFORTS` / `zCodex` lama) dengan:

```ts
// SPEC-338 · ADR-0074 · katalog codex. Slug diteruskan apa adanya ke `codex -m`; effort ke
// `-c model_reasoning_effort="<v>"` (codex tak punya flag effort).
// SPEC-339 · effort adalah properti MODEL, bukan properti CLI. GPT-5.6 menambah `max` (kedalaman
// nalar maksimum) dan `ultra` (nalar maksimum + delegasi tugas ke subagent), TAPI Luna tak
// mendukung `ultra` dan seluruh model 5.5 ke bawah tak mendukung keduanya. Dua daftar sejajar
// karena itu tak lagi cukup — tiap model membawa daftar effort-nya sendiri.
// Nilai diverifikasi terhadap `codex debug models` (codex-cli 0.145.0) dan
// codex-rs/models-manager/models.json upstream.
export type CodexModel = {
  id: string; label: string;
  /** Effort yang didukung model ini, urut kuat → ringan. */
  efforts: readonly string[];
  /** Dipakai bila effort tersimpan tak didukung model ini. Wajib ada di `efforts`. */
  fallback: string;
  /** `minimal_client_version` dari manifest codex — dasar peringatan versi lunak (SPEC-339). */
  minClient: string;
};

const E_5_6 = ["ultra", "max", "xhigh", "high", "medium", "low"] as const;
const E_LUNA = ["max", "xhigh", "high", "medium", "low"] as const;
// Irisan yang didukung SETIAP model codex — juga jawaban untuk model yang belum kita daftar.
const E_BASE = ["xhigh", "high", "medium", "low"] as const;

export const CODEX_MODELS: readonly CodexModel[] = [
  { id: "gpt-5.6-sol",   label: "GPT-5.6 Sol",   efforts: E_5_6,  fallback: "xhigh", minClient: "0.144.0" },
  { id: "gpt-5.6-terra", label: "GPT-5.6 Terra", efforts: E_5_6,  fallback: "xhigh", minClient: "0.144.0" },
  { id: "gpt-5.6-luna",  label: "GPT-5.6 Luna",  efforts: E_LUNA, fallback: "xhigh", minClient: "0.144.0" },
  { id: "gpt-5.5",       label: "GPT-5.5",       efforts: E_BASE, fallback: "xhigh", minClient: "0.124.0" },
];

// Gabungan semua effort codex. Dipertahankan demi pemanggil lama, TAPI bukan lagi sumber pilihan
// UI — picker wajib memakai `codexEfforts(model)` supaya kombinasi tolak tak pernah tampil.
export const CODEX_EFFORTS = E_5_6;

export function codexModel(id: string): CodexModel | undefined {
  return CODEX_MODELS.find((m) => m.id === id);
}

/** Effort yang boleh dipilih untuk sebuah model. Model tak dikenal → irisan aman, bukan kosong. */
export function codexEfforts(modelId: string): readonly string[] {
  return codexModel(modelId)?.efforts ?? E_BASE;
}

/**
 * Turunkan `effort` ke nilai yang benar-benar didukung `modelId`. Model tak dikenal meneruskan
 * effort apa adanya: katalog ini kurasi UI, bukan gerbang validasi — server sengaja lenient
 * (`z.string()`), dan model baru yang belum sempat didaftar tak boleh jadi mustahil dijalankan.
 */
export function coerceCodexEffort(modelId: string, effort: string): string {
  const m = codexModel(modelId);
  if (!m) return effort;
  return m.efforts.includes(effort) ? effort : m.fallback;
}

/**
 * Model yang tak lagi ada di picker → penggantinya saat dibaca (cermin RETIRED_MODELS milik claude).
 * Semua dipetakan ke `gpt-5.5`, SENGAJA bukan ke 5.6: gpt-5.5 hanya butuh klien 0.124.0, sedangkan
 * trio 5.6 butuh 0.144.0. Pensiun tak boleh memindahkan setelan orang ke model yang CLI-nya belum
 * sanggup menjalankannya.
 */
export const RETIRED_CODEX_MODELS: Record<string, string> = {
  "gpt-5.4": "gpt-5.5",
  "gpt-5.4-mini": "gpt-5.5",
  "gpt-5.3-codex-spark": "gpt-5.5",
};

// Default model/effort codex. Model/effort claude sengaja TETAP di `Setting.model`/`Setting.effort`
// (kontrak GET /settings + baris Setting lama), jadi blok ini hanya untuk codex.
export const zCodex = z.object({
  model: z.string().default("gpt-5.6-sol"),
  effort: z.string().default("xhigh"),
});
export type Codex = z.infer<typeof zCodex>;
export const CODEX_DEFAULTS: Codex = zCodex.parse({});
```

- [x] **Step 4: Jalankan test, pastikan LULUS**

Run: `env -u NODE_ENV DATABASE_URL=postgresql://hanoman:hanoman@localhost:5432/hanoman339 pnpm vitest run --no-file-parallelism shared/src/codex-catalog.test.ts`
Expected: PASS (8 test).

- [x] **Step 5: Perbarui test SPEC-338 yang kini memegang nilai lama**

Di `shared/src/agent-session.test.ts`, ganti dua kasus (baris 12–20) menjadi:

```ts
  it("default codex = gpt-5.6-sol / xhigh", () => {
    expect(CODEX_DEFAULTS).toEqual({ model: "gpt-5.6-sol", effort: "xhigh" });
    expect(zCodex.parse({})).toEqual(CODEX_DEFAULTS);
  });

  it("katalog codex memuat slug & effort yang didukung CLI", () => {
    expect(CODEX_MODELS.map((m) => m.id)).toContain("gpt-5.6-sol");
    // SPEC-339 · CODEX_EFFORTS kini GABUNGAN semua effort; pilihan per model ada di codexEfforts().
    expect(CODEX_EFFORTS).toEqual(["ultra", "max", "xhigh", "high", "medium", "low"]);
  });
```

- [x] **Step 6: Jalankan seluruh test `shared` + typecheck**

Run: `env -u NODE_ENV DATABASE_URL=postgresql://hanoman:hanoman@localhost:5432/hanoman339 pnpm vitest run --no-file-parallelism shared && pnpm --filter ./shared typecheck`
Expected: PASS, exit 0. (Jangan pernah `tsc -p .` tanpa `--noEmit` di `shared` — ia mengotori `src/` dengan puluhan `.js`/`.d.ts`.)

- [x] **Step 7: Commit**

```bash
git add shared/src/entities.ts shared/src/codex-catalog.test.ts shared/src/agent-session.test.ts
git commit -m "feat(spec-339): katalog codex per-model + effort max & ultra"
```

---

### Task 2: `getSetting()` meremap model pensiun & mengoreksi effort

**Files:**
- Modify: `server/src/services/settings.ts:1-34`
- Modify: `server/test/settings.test.ts`

**Interfaces:**
- Consumes: `RETIRED_CODEX_MODELS`, `coerceCodexEffort` (Task 1).
- Produces: `getSetting()` yang blok `codex`-nya selalu sudah ternormalisasi.

- [x] **Step 1: Tulis test yang gagal**

Tambahkan ke `server/test/settings.test.ts` (di dalam `describe` teratas berkas itu; sesuaikan nama impor `getSetting` bila berkas sudah mengimpornya):

```ts
  // SPEC-339 · baris Setting lama menyimpan model codex yang sudah dipensiunkan. Dibaca mentah,
  // UI menampilkan Select kosong dan sesi lahir dengan model yang tak ada di picker.
  it("meremap model codex pensiun ke gpt-5.5 saat dibaca", async () => {
    await prisma.setting.upsert({
      where: { id: 1 },
      create: { id: 1, data: { ...DEFAULT_SETTING, codex: { model: "gpt-5.3-codex-spark", effort: "high" } } },
      update: { data: { ...DEFAULT_SETTING, codex: { model: "gpt-5.3-codex-spark", effort: "high" } } },
    });
    const s = await getSetting();
    expect(s.codex).toEqual({ model: "gpt-5.5", effort: "high" });
  });

  it("mengoreksi effort yang tak didukung model tersimpan", async () => {
    await prisma.setting.upsert({
      where: { id: 1 },
      create: { id: 1, data: { ...DEFAULT_SETTING, codex: { model: "gpt-5.6-luna", effort: "ultra" } } },
      update: { data: { ...DEFAULT_SETTING, codex: { model: "gpt-5.6-luna", effort: "ultra" } } },
    });
    const s = await getSetting();
    expect(s.codex).toEqual({ model: "gpt-5.6-luna", effort: "xhigh" });
  });

  // Pensiun + koersi harus berurutan: effort divalidasi terhadap model HASIL pemetaan.
  it("model pensiun + effort mustahil → keduanya dibereskan sekaligus", async () => {
    await prisma.setting.upsert({
      where: { id: 1 },
      create: { id: 1, data: { ...DEFAULT_SETTING, codex: { model: "gpt-5.4", effort: "ultra" } } },
      update: { data: { ...DEFAULT_SETTING, codex: { model: "gpt-5.4", effort: "ultra" } } },
    });
    const s = await getSetting();
    expect(s.codex).toEqual({ model: "gpt-5.5", effort: "xhigh" });
  });
```

Bila `prisma`, `getSetting`, atau `DEFAULT_SETTING` belum diimpor di berkas itu, tambahkan:

```ts
import { prisma } from "../src/db";
import { getSetting, DEFAULT_SETTING } from "../src/services/settings";
```

- [x] **Step 2: Jalankan test, pastikan GAGAL**

Run:
```bash
env -u NODE_ENV DATABASE_URL=postgresql://hanoman:hanoman@localhost:5432/hanoman339 \
  pnpm vitest run --no-file-parallelism server/test/settings.test.ts
```
Expected: FAIL — diterima `{ model: "gpt-5.3-codex-spark", effort: "high" }`.

- [x] **Step 3: Implementasi minimal**

Di `server/src/services/settings.ts`, perluas impor baris 2:

```ts
import {
  zSetting, SCHEDULER_DEFAULTS, GOAL_DEFAULTS, CODEX_DEFAULTS, RETIRED_CODEX_MODELS,
  coerceCodexEffort, type Setting, type Agent, type Codex,
} from "@hanoman/shared";
```

Ganti badan `getSetting()` (baris 25–30) menjadi:

```ts
export async function getSetting(): Promise<Setting> {
  const raw = (await prisma.setting.findUnique({ where: { id: 1 } }))?.data;
  if (raw === undefined || raw === null) return DEFAULT_SETTING;
  const parsed = zSetting.safeParse(raw);
  if (!parsed.success) return DEFAULT_SETTING;
  return {
    ...parsed.data,
    model: RETIRED_MODELS[parsed.data.model] ?? parsed.data.model,
    codex: normalizeCodex(parsed.data.codex),
  };
}

/**
 * SPEC-339 · cermin RETIRED_MODELS untuk blok codex, plus koersi effort. Urutannya penting:
 * effort divalidasi terhadap model HASIL pemetaan, bukan model tersimpan — memetakan
 * `gpt-5.4` (tanpa ultra) ke gpt-5.5 tak ada gunanya bila effort `ultra`-nya dibiarkan.
 */
function normalizeCodex(c: Codex): Codex {
  const model = RETIRED_CODEX_MODELS[c.model] ?? c.model;
  return { model, effort: coerceCodexEffort(model, c.effort) };
}
```

- [x] **Step 4: Jalankan test, pastikan LULUS**

Run:
```bash
env -u NODE_ENV DATABASE_URL=postgresql://hanoman:hanoman@localhost:5432/hanoman339 \
  pnpm vitest run --no-file-parallelism server/test/settings.test.ts
```
Expected: PASS.

- [x] **Step 5: Commit**

```bash
git add server/src/services/settings.ts server/test/settings.test.ts
git commit -m "feat(spec-339): remap model codex pensiun + koersi effort saat baca Setting"
```

---

### Task 3: Koersi effort di `createSession` — titik cekik tunggal

**Files:**
- Modify: `server/src/services/pty.ts:215-239`
- Modify: `server/test/pty.test.ts`

**Interfaces:**
- Consumes: `coerceCodexEffort` (Task 1).
- Produces: jaminan bahwa argv sesi codex tak pernah memuat effort yang tak didukung model-nya, untuk SEMUA pemanggil (`routes/terminal.ts`, `routes/ide.ts`, `routes/vps.ts`, `services/session-launch.ts`, scheduler, dan `POST` ber-`AgentToken`).

- [x] **Step 1: Tulis test yang gagal**

Tambahkan ke `server/test/pty.test.ts` di dalam `describe("pty service", …)`:

```ts
  // SPEC-339 · koersi ditaruh di createSession, bukan di route: SEMUA kelahiran sesi lewat sini,
  // termasuk POST ber-AgentToken yang tak pernah menyentuh picker UI.
  it("effort yang tak didukung model codex diturunkan sebelum masuk argv", async () => {
    process.env.HANOMAN_CODEX_BIN = "/bin/echo";
    const s = createSession("p-339", process.cwd(), {
      agent: "codex", model: "gpt-5.6-luna", effort: "ultra",
    });
    await waitFor(() => exited(s.id));
    const c = fakeClient();
    attach(s.id, c);
    // tmux membungkus baris pada lebar viewport — buang seluruh whitespace sebelum mencocokkan,
    // kalau tidak token bisa terpotong di tengah dan assert-nya gagal palsu.
    const flat = allData(c).replace(/\s+/g, "");
    expect(flat).toContain('model_reasoning_effort="xhigh"');
    expect(flat).not.toContain('model_reasoning_effort="ultra"');
  });

  it("effort yang didukung diteruskan apa adanya", async () => {
    process.env.HANOMAN_CODEX_BIN = "/bin/echo";
    const s = createSession("p-339b", process.cwd(), {
      agent: "codex", model: "gpt-5.6-sol", effort: "ultra",
    });
    await waitFor(() => exited(s.id));
    const c = fakeClient();
    attach(s.id, c);
    expect(allData(c).replace(/\s+/g, "")).toContain('model_reasoning_effort="ultra"');
  });

  it("sesi claude tak tersentuh koersi codex", async () => {
    process.env.HANOMAN_CLAUDE_BIN = "/bin/echo";
    const s = createSession("p-339c", process.cwd(), { model: "claude-opus-5", effort: "ultracode" });
    await waitFor(() => exited(s.id));
    const c = fakeClient();
    attach(s.id, c);
    expect(allData(c).replace(/\s+/g, "")).toContain("--effortultracode");
  });
```

- [x] **Step 2: Jalankan test, pastikan GAGAL**

Run:
```bash
env -u NODE_ENV DATABASE_URL=postgresql://hanoman:hanoman@localhost:5432/hanoman339 \
  pnpm vitest run --no-file-parallelism server/test/pty.test.ts
```
Expected: FAIL pada kasus pertama — argv masih memuat `model_reasoning_effort="ultra"`.

- [x] **Step 3: Implementasi minimal**

Di `server/src/services/pty.ts`, tambahkan `coerceCodexEffort` ke impor `@hanoman/shared` yang sudah ada, lalu di dalam blok `else` `createSession` (tepat sebelum pemanggilan `agentFlags`, baris ~234) sisipkan:

```ts
    // SPEC-339 · titik cekik tunggal: effort yang tak didukung model codex diturunkan ke fallback
    // model SEBELUM argv dirakit. Ditaruh di sini, bukan di route, karena SEMUA kelahiran sesi
    // bermuara ke createSession — termasuk jalur ber-AgentToken yang tak lewat picker UI.
    // Hanya dikoersi bila keduanya ada: tanpa effort, `agentFlags` memang tak memasang flag apa pun.
    const effort = agent === "codex" && opts.model && opts.effort
      ? coerceCodexEffort(opts.model, opts.effort)
      : opts.effort;
```

lalu ubah pemanggilan `agentFlags` agar memakai `effort` hasil koersi:

```ts
    const flags = agentFlags({
      agent, model: opts.model, effort,
      decisionFile: opts.decisionFile, goal: opts.goal, goalGate,
    }).map(sq).join(" ");
```

- [x] **Step 4: Jalankan test, pastikan LULUS**

Run:
```bash
env -u NODE_ENV DATABASE_URL=postgresql://hanoman:hanoman@localhost:5432/hanoman339 \
  pnpm vitest run --no-file-parallelism server/test/pty.test.ts
```
Expected: PASS.

- [x] **Step 5: Commit**

```bash
git add server/src/services/pty.ts server/test/pty.test.ts
git commit -m "feat(spec-339): koersi effort codex di createSession sebagai titik cekik tunggal"
```

---

### Task 4: Deteksi versi codex + `GET /api/codex/version`

**Files:**
- Create: `server/src/services/codex-version.ts`
- Create: `server/src/routes/codex.ts`
- Create: `server/test/codex-version.test.ts`
- Modify: `server/src/app.ts:17` (impor) dan `:146` (register)
- Modify: `shared/src/api.ts:20` (tambah path)
- Modify: `src/src/api/client.ts:116` (tambah method)

**Interfaces:**
- Produces: `CODEX_MIN_CLIENT = "0.144.0"`; `parseCodexVersion(out: string): string | null`; `cmpVersion(a: string, b: string): number`; `codexVersionInfo(): Promise<{ version: string | null; minRequired: string; ok: boolean }>`; `_resetCodexVersionCache(): void`; `paths.codexVersion`; `api.getCodexVersion()`.

- [x] **Step 1: Tulis test yang gagal**

Buat `server/test/codex-version.test.ts`:

```ts
import { describe, it, expect, beforeEach, afterEach } from "vitest";
import {
  parseCodexVersion, cmpVersion, codexVersionInfo, _resetCodexVersionCache, CODEX_MIN_CLIENT,
} from "../src/services/codex-version";

beforeEach(() => { _resetCodexVersionCache(); });
afterEach(() => { delete process.env.HANOMAN_CODEX_BIN; });

describe("SPEC-339 · deteksi versi codex", () => {
  it("memparse keluaran `codex --version`", () => {
    expect(parseCodexVersion("codex-cli 0.145.0\n")).toBe("0.145.0");
    expect(parseCodexVersion("codex-cli 0.142.5")).toBe("0.142.5");
    expect(parseCodexVersion("bukan versi apa pun")).toBeNull();
  });

  // localeCompare akan bilang "0.9.0" > "0.144.0" — perbandingan WAJIB numerik per segmen.
  it("membandingkan versi secara numerik per segmen", () => {
    expect(cmpVersion("0.142.5", "0.144.0")).toBeLessThan(0);
    expect(cmpVersion("0.145.0", "0.144.0")).toBeGreaterThan(0);
    expect(cmpVersion("0.144.0", "0.144.0")).toBe(0);
    expect(cmpVersion("0.9.0", "0.144.0")).toBeLessThan(0);
  });

  it("biner yang melaporkan versi cukup baru → ok", async () => {
    process.env.HANOMAN_CODEX_BIN = "/bin/echo";   // mencetak argumennya: "--version"
    // /bin/echo tak memuat angka versi → version null, dan null TIDAK dianggap gagal.
    const info = await codexVersionInfo();
    expect(info.minRequired).toBe(CODEX_MIN_CLIENT);
    expect(info.version).toBeNull();
    expect(info.ok).toBe(true);
  });

  it("biner tak ada → version null, ok true (ketiadaan bukti bukan bukti ketiadaan)", async () => {
    process.env.HANOMAN_CODEX_BIN = "/tak/ada/codex-339";
    const info = await codexVersionInfo();
    expect(info.version).toBeNull();
    expect(info.ok).toBe(true);
  });
});
```

- [x] **Step 2: Jalankan test, pastikan GAGAL**

Run:
```bash
env -u NODE_ENV DATABASE_URL=postgresql://hanoman:hanoman@localhost:5432/hanoman339 \
  pnpm vitest run --no-file-parallelism server/test/codex-version.test.ts
```
Expected: FAIL — `Cannot find module '../src/services/codex-version'`.

- [x] **Step 3: Tulis service**

Buat `server/src/services/codex-version.ts`:

```ts
import { execFile } from "node:child_process";
import { promisify } from "node:util";
import { effectiveStr } from "../config";

const run = promisify(execFile);

// SPEC-339 · GPT-5.6 (sol/terra/luna) baru muncul di manifest untuk klien >= 0.144.0. Manifest
// disaring DI SISI SERVER berdasarkan versi klien, jadi CLI lama tak melihat model-model itu sama
// sekali — bukan soal langganan, murni gerbang versi. `max` pun baru ada di enum effort 0.144+.
export const CODEX_MIN_CLIENT = "0.144.0";

// TTL, bukan cache selamanya: mesin yang baru di-upgrade harus berhenti diperingatkan tanpa perlu
// restart server, sementara 5 menit cukup panjang agar endpoint tak men-spawn proses tiap render.
const TTL_MS = 5 * 60_000;
let cache: { at: number; version: string | null } | null = null;

const codexBin = (): string => effectiveStr("HANOMAN_CODEX_BIN") ?? "codex";

/** Ambil `X.Y.Z` pertama dari keluaran `codex --version` (mis. "codex-cli 0.145.0"). */
export function parseCodexVersion(out: string): string | null {
  const m = /(\d+)\.(\d+)\.(\d+)/.exec(out);
  return m ? `${m[1]}.${m[2]}.${m[3]}` : null;
}

/** Perbandingan numerik per segmen. String compare salah: "0.9.0" > "0.144.0" secara leksikal. */
export function cmpVersion(a: string, b: string): number {
  const pa = a.split(".").map(Number);
  const pb = b.split(".").map(Number);
  for (let i = 0; i < Math.max(pa.length, pb.length); i++) {
    const d = (pa[i] ?? 0) - (pb[i] ?? 0);
    if (d !== 0) return d;
  }
  return 0;
}

/**
 * Versi codex CLI terpasang, atau `null` bila biner tak ada / tak bisa dijalankan / keluarannya
 * tak dikenali. Gagal-diam: ini observabilitas, bukan gerbang — kegagalannya tak boleh membuat
 * endpoint 500 apalagi menghalangi kelahiran sesi.
 */
export async function getCodexVersion(now = Date.now()): Promise<string | null> {
  if (cache && now - cache.at < TTL_MS) return cache.version;
  let version: string | null = null;
  try {
    const { stdout } = await run(codexBin(), ["--version"], { timeout: 5_000 });
    version = parseCodexVersion(stdout);
  } catch { /* biner tak ada / gagal jalan — biarkan null */ }
  cache = { at: now, version };
  return version;
}

/**
 * `ok` = false HANYA bila versi benar-benar terdeteksi DAN lebih rendah dari minimum. `version`
 * null → `ok` true: ketiadaan bukti bukan bukti ketiadaan, dan peringatan palsu lebih merusak
 * kepercayaan pada indikator daripada tak ada indikator sama sekali.
 */
export async function codexVersionInfo(): Promise<{ version: string | null; minRequired: string; ok: boolean }> {
  const version = await getCodexVersion();
  return { version, minRequired: CODEX_MIN_CLIENT, ok: version === null || cmpVersion(version, CODEX_MIN_CLIENT) >= 0 };
}

/** Untuk test: kosongkan cache antar kasus. */
export function _resetCodexVersionCache(): void { cache = null; }
```

- [x] **Step 4: Jalankan test, pastikan LULUS**

Run:
```bash
env -u NODE_ENV DATABASE_URL=postgresql://hanoman:hanoman@localhost:5432/hanoman339 \
  pnpm vitest run --no-file-parallelism server/test/codex-version.test.ts
```
Expected: PASS.

- [x] **Step 5: Pasang endpoint**

Buat `server/src/routes/codex.ts`:

```ts
import type { FastifyInstance } from "fastify";
import { codexVersionInfo } from "../services/codex-version";

// GET /api/codex/version (SPEC-339) — versi codex CLI terpasang + minimum yang dibutuhkan trio
// GPT-5.6. Murni observabilitas untuk peringatan lunak di UI: TIDAK memblokir kelahiran sesi
// (ADR-0037 — agen dipercaya, isolasi lewat worktree).
export default async function codex(app: FastifyInstance) {
  app.get("/codex/version", async () => codexVersionInfo());
}
```

Di `server/src/app.ts`, tambahkan impor di dekat baris 17:

```ts
import codex from "./routes/codex";
```

dan register setelah `audit` (baris ~146):

```ts
    await api.register(codex);      // SPEC-339 · versi codex CLI untuk peringatan model 5.6
```

Di `shared/src/api.ts`, setelah baris `limits`:

```ts
  // SPEC-339 · versi codex CLI (peringatan lunak model GPT-5.6).
  codexVersion: `${API}/codex/version`,
```

Di `src/src/api/client.ts`, setelah `putSettings` (baris 117):

```ts
  // SPEC-339 · versi codex CLI; dipakai catatan "CLI terlalu tua" di Settings & picker Start.
  getCodexVersion: () => j<{ version: string | null; minRequired: string; ok: boolean }>(paths.codexVersion),
```

- [x] **Step 6: Verifikasi endpoint hidup lewat test route**

Tambahkan ke `server/test/codex-version.test.ts`:

```ts
import { build } from "../src/app";

it("GET /api/codex/version mengembalikan bentuk kontraknya", async () => {
  const app = await build();
  const res = await app.inject({ method: "GET", url: "/api/codex/version" });
  expect([200, 401]).toContain(res.statusCode);   // 401 bila gate cookie aktif di lingkungan test
  if (res.statusCode === 200) {
    expect(res.json()).toMatchObject({ minRequired: "0.144.0" });
  }
  await app.close();
});
```

Bila `server/test/app.test.ts` memperlihatkan cara membangun app + cookie auth yang berbeda, ikuti pola berkas itu alih-alih `build()` polos.

Run:
```bash
env -u NODE_ENV DATABASE_URL=postgresql://hanoman:hanoman@localhost:5432/hanoman339 \
  pnpm vitest run --no-file-parallelism server/test/codex-version.test.ts
```
Expected: PASS.

- [x] **Step 7: Commit**

```bash
git add server/src/services/codex-version.ts server/src/routes/codex.ts server/test/codex-version.test.ts \
        server/src/app.ts shared/src/api.ts src/src/api/client.ts
git commit -m "feat(spec-339): deteksi versi codex CLI + GET /api/codex/version"
```

---

### Task 5: Picker effort mengikuti model (StartSessionModal + Settings)

**Files:**
- Modify: `src/src/App.tsx:78-79` dan handler `onChange` model
- Modify: `src/src/screens/SettingsScreen.tsx:534-543`
- Modify: `src/test/start-session-agent.test.tsx:36-52`
- Modify: `src/test/settings-agent.test.tsx:13-53`

**Interfaces:**
- Consumes: `codexEfforts`, `coerceCodexEffort`, `CODEX_MODELS`, `CODEX_DEFAULTS` (Task 1).

- [x] **Step 1: Tulis test yang gagal**

Di `src/test/start-session-agent.test.tsx`, ganti fixture `codex` di helper `settings` (baris 17) menjadi `codex: { model: "gpt-5.6-sol", effort: "xhigh" }`, perbaiki ekspektasi yang menyebut `gpt-5.5`/`gpt-5.4` menjadi slug baru, lalu tambahkan:

```ts
  it("daftar effort menyempit mengikuti model: Luna tanpa ultra", async () => {
    render(<StartSessionModal open spec={spec} onClose={() => {}} onStarted={() => {}} />);
    await waitFor(() => expect(screen.getByLabelText("Model")).toHaveValue("claude-opus-5"));
    fireEvent.change(screen.getByLabelText("Agen"), { target: { value: "codex" } });
    await waitFor(() => expect(screen.getByLabelText("Model")).toHaveValue("gpt-5.6-sol"));

    const effortsOf = () =>
      [...screen.getByLabelText("Effort").querySelectorAll("option")].map((o) => o.value);
    expect(effortsOf()).toEqual(["ultra", "max", "xhigh", "high", "medium", "low"]);

    fireEvent.change(screen.getByLabelText("Model"), { target: { value: "gpt-5.6-luna" } });
    await waitFor(() => expect(screen.getByLabelText("Model")).toHaveValue("gpt-5.6-luna"));
    expect(effortsOf()).toEqual(["max", "xhigh", "high", "medium", "low"]);
  });

  // Menurunkan effort HARUS terlihat di picker, bukan terjadi diam-diam saat sesi lahir.
  it("effort tak didukung turun ke xhigh saat model ditukar", async () => {
    render(<StartSessionModal open spec={spec} onClose={() => {}} onStarted={() => {}} />);
    await waitFor(() => expect(screen.getByLabelText("Model")).toHaveValue("claude-opus-5"));
    fireEvent.change(screen.getByLabelText("Agen"), { target: { value: "codex" } });
    await waitFor(() => expect(screen.getByLabelText("Model")).toHaveValue("gpt-5.6-sol"));
    fireEvent.change(screen.getByLabelText("Effort"), { target: { value: "ultra" } });
    fireEvent.change(screen.getByLabelText("Model"), { target: { value: "gpt-5.6-luna" } });
    await waitFor(() => expect(screen.getByLabelText("Effort")).toHaveValue("xhigh"));
  });
```

Di `src/test/settings-agent.test.tsx`, ganti fixture `codex` (baris 17) menjadi `{ model: "gpt-5.6-sol", effort: "xhigh" }`, ganti kasus baris 39–53 agar memakai slug baru, lalu tambahkan:

```ts
  it("memilih Luna saat effort ultra ikut menurunkan effort di PUT", async () => {
    vi.mocked(api.getSettings).mockResolvedValue(
      settings({ codex: { model: "gpt-5.6-sol", effort: "ultra" } }) as any);
    openModel();
    const sel = await screen.findByLabelText("Model codex");
    fireEvent.change(sel, { target: { value: "gpt-5.6-luna" } });
    await waitFor(() => expect(api.putSettings).toHaveBeenCalledWith(
      expect.objectContaining({ codex: { model: "gpt-5.6-luna", effort: "xhigh" } })));
  });

  it("daftar effort codex mengikuti model terpilih", async () => {
    openModel();
    await screen.findByLabelText("Effort codex");
    const opts = [...screen.getByLabelText("Effort codex").querySelectorAll("option")].map((o) => o.value);
    expect(opts).toEqual(["ultra", "max", "xhigh", "high", "medium", "low"]);
  });
```

- [x] **Step 2: Jalankan test, pastikan GAGAL**

Run: `env -u NODE_ENV DATABASE_URL=postgresql://hanoman:hanoman@localhost:5432/hanoman339 pnpm vitest run --no-file-parallelism src/test/start-session-agent.test.tsx src/test/settings-agent.test.tsx`
Expected: FAIL — daftar effort masih `["xhigh","high","medium","low"]` untuk semua model.

- [x] **Step 3: Implementasi di `StartSessionModal`**

Di `src/src/App.tsx` perluas impor `@hanoman/shared` dengan `codexEfforts` dan `coerceCodexEffort`, lalu ganti baris 78–79:

```ts
  const models: readonly { id: string; label: string }[] = agent === "codex" ? CODEX_MODELS : MODELS;
  // SPEC-339 · effort adalah properti MODEL untuk codex — daftarnya menyempit mengikuti pilihan.
  const efforts: readonly string[] = agent === "codex" ? codexEfforts(model) : EFFORTS;
  // Menukar model bisa membuat effort terpilih jadi tak sah (Luna tak punya `ultra`). Turunkan
  // SEKARANG supaya perubahannya terlihat di picker, bukan diam-diam saat sesi lahir.
  const pickModel = (id: string) => {
    setModel(id);
    if (agent === "codex") setEffort((e) => coerceCodexEffort(id, e));
  };
```

lalu pakai `pickModel` pada Select "Model" (baris ~117):

```tsx
          onChange={(e: React.ChangeEvent<HTMLSelectElement>) => pickModel(e.target.value)} />
```

`pickAgent` (baris 77) juga harus mengoreksi effort saat berpindah ke codex:

```ts
  const pickAgent = (a: Agent) => {
    setAgent(a);
    setModel(defs[a].model);
    setEffort(a === "codex" ? coerceCodexEffort(defs[a].model, defs[a].effort) : defs[a].effort);
  };
```

- [x] **Step 4: Implementasi di `SettingsScreen`**

Di `src/src/screens/SettingsScreen.tsx` perluas impor `@hanoman/shared` dengan `codexEfforts` dan `coerceCodexEffort`, lalu ganti dua `SettingRow` codex (baris 534–543):

```tsx
        <SettingRow title="Model codex" desc="Diteruskan apa adanya ke `codex -m`.">
          <Select size="sm" aria-label="Model codex" value={codex.model} style={{ width: 190 }}
            options={CODEX_MODELS.map((m) => ({ value: m.id, label: m.label }))}
            onChange={(e) => {
              // SPEC-339 · effort ikut dikoreksi: memilih Luna saat effort `ultra` harus menyimpan
              // pasangan yang sah, bukan pasangan yang nanti ditolak codex saat sesi lahir.
              const model = e.target.value;
              save({ codex: { model, effort: coerceCodexEffort(model, codex.effort) } }, "Model codex → " + model);
            }} />
        </SettingRow>
        <SettingRow title="Effort codex" last desc="Diteruskan ke `codex -c model_reasoning_effort`.">
          <Select size="sm" aria-label="Effort codex" value={codex.effort} style={{ width: 130 }}
            options={codexEfforts(codex.model).map((v) => ({ value: v, label: v }))}
            onChange={(e) => save({ codex: { ...codex, effort: e.target.value } }, "Effort codex → " + e.target.value)} />
        </SettingRow>
```

- [x] **Step 5: Jalankan test, pastikan LULUS**

Run: `env -u NODE_ENV DATABASE_URL=postgresql://hanoman:hanoman@localhost:5432/hanoman339 pnpm vitest run --no-file-parallelism src/test/start-session-agent.test.tsx src/test/settings-agent.test.tsx`
Expected: PASS.

- [x] **Step 6: Tulis test untuk model di luar katalog**

`codex.model` tersimpan bisa berada di luar katalog — datang dari `PUT /api/settings` ber-`AgentToken`, atau model baru yang belum sempat didaftar. Dibiarkan, Select-nya tampil kosong dan pengguna tak bisa tahu model apa yang sedang berlaku.

Tambahkan ke `src/test/settings-agent.test.tsx`:

```ts
  // SPEC-339 · nilai di luar katalog tetap harus TERLIHAT, bukan membuat Select kosong.
  it("model codex di luar katalog tetap tampil sebagai opsi", async () => {
    vi.mocked(api.getSettings).mockResolvedValue(
      settings({ codex: { model: "gpt-7-belum-ada", effort: "ultra" } }) as any);
    openModel();
    const sel = await screen.findByLabelText("Model codex");
    expect(sel).toHaveValue("gpt-7-belum-ada");
    const opts = [...sel.querySelectorAll("option")].map((o) => o.value);
    expect(opts).toContain("gpt-7-belum-ada");
  });
```

- [x] **Step 7: Jalankan test, pastikan GAGAL**

Run: `env -u NODE_ENV DATABASE_URL=postgresql://hanoman:hanoman@localhost:5432/hanoman339 pnpm vitest run --no-file-parallelism src/test/settings-agent.test.tsx`
Expected: FAIL — Select tak memuat opsi `gpt-7-belum-ada`.

- [x] **Step 8: Implementasi opsi luar katalog**

Di `src/src/screens/SettingsScreen.tsx`, ganti `options` pada Select "Model codex":

```tsx
            options={(CODEX_MODELS.some((m) => m.id === codex.model)
              ? CODEX_MODELS.map((m) => ({ value: m.id, label: m.label }))
              // Nilai di luar katalog (mis. dari PUT ber-AgentToken) ditambahkan apa adanya supaya
              // picker tak tampil kosong. Effort-nya pun tak dikoersi — konsisten dengan aturan
              // "model tak dikenal → apa adanya" di coerceCodexEffort.
              : [{ value: codex.model, label: codex.model },
                 ...CODEX_MODELS.map((m) => ({ value: m.id, label: m.label }))])}
```

- [x] **Step 9: Jalankan test, pastikan LULUS**

Run: `env -u NODE_ENV DATABASE_URL=postgresql://hanoman:hanoman@localhost:5432/hanoman339 pnpm vitest run --no-file-parallelism src/test/settings-agent.test.tsx`
Expected: PASS.

- [x] **Step 10: Commit**

```bash
git add src/src/App.tsx src/src/screens/SettingsScreen.tsx \
        src/test/start-session-agent.test.tsx src/test/settings-agent.test.tsx
git commit -m "feat(spec-339): picker effort codex mengikuti model terpilih"
```

---

### Task 6: Catatan versi lunak di UI

**Files:**
- Modify: `src/src/screens/SettingsScreen.tsx` (kartu "Agen sesi")
- Create: `src/test/codex-version-note.test.tsx`

**Interfaces:**
- Consumes: `api.getCodexVersion()` (Task 4), `codexModel()` (Task 1).

- [x] **Step 1: Tulis test yang gagal**

Buat `src/test/codex-version-note.test.tsx`:

```tsx
import { describe, it, expect, vi, beforeEach } from "vitest";
import { render, screen, fireEvent, waitFor } from "@testing-library/react";

vi.mock("../src/api/client", () => ({
  api: { getSettings: vi.fn(), putSettings: vi.fn(), getCodexVersion: vi.fn() },
  ApiError: class extends Error { status = 0 },
}));

import { SettingsScreen } from "../src/screens/SettingsScreen";
import { api } from "../src/api/client";

const me: any = { id: "u1", email: "dena@nafanesia.id", createdAt: "x" };
const settings = (over: object = {}) => ({
  model: "claude-opus-5", effort: "xhigh", autoDefault: true, autoScaffold: true, notifyFail: true,
  notifyDone: true, notifySound: "short", notifyDecision: true, notifyDecisionSound: "alert",
  agentAccessEnabled: false, scheduler: {}, goal: { enabled: false, condition: "" },
  agent: "claude", codex: { model: "gpt-5.6-sol", effort: "xhigh" }, ...over,
});

beforeEach(() => {
  vi.mocked(api.getSettings).mockResolvedValue(settings() as any);
  vi.mocked(api.putSettings).mockResolvedValue(settings() as any);
});

const openModel = () => {
  render(<SettingsScreen me={me} onLoggedOut={() => {}} />);
  fireEvent.click(screen.getByRole("button", { name: "Model sesi" }));
};

describe("SPEC-339 · catatan versi codex", () => {
  it("CLI terlalu tua untuk model 5.6 → catatan muncul dengan perintah upgrade", async () => {
    vi.mocked(api.getCodexVersion).mockResolvedValue(
      { version: "0.142.5", minRequired: "0.144.0", ok: false } as any);
    openModel();
    const note = await screen.findByTestId("codex-version-note");
    expect(note).toHaveTextContent("0.142.5");
    expect(note).toHaveTextContent("0.144.0");
  });

  it("CLI cukup baru → tak ada catatan", async () => {
    vi.mocked(api.getCodexVersion).mockResolvedValue(
      { version: "0.145.0", minRequired: "0.144.0", ok: true } as any);
    openModel();
    await screen.findByLabelText("Model codex");
    expect(screen.queryByTestId("codex-version-note")).toBeNull();
  });

  // Ketiadaan bukti bukan bukti ketiadaan: versi tak terdeteksi TIDAK boleh memicu peringatan.
  it("versi tak terdeteksi → tak ada catatan", async () => {
    vi.mocked(api.getCodexVersion).mockResolvedValue(
      { version: null, minRequired: "0.144.0", ok: true } as any);
    openModel();
    await screen.findByLabelText("Model codex");
    expect(screen.queryByTestId("codex-version-note")).toBeNull();
  });

  // Model yang tak butuh 0.144.0 tak boleh ikut diperingatkan.
  it("model gpt-5.5 pada CLI 0.142.5 → tak ada catatan", async () => {
    vi.mocked(api.getSettings).mockResolvedValue(
      settings({ codex: { model: "gpt-5.5", effort: "xhigh" } }) as any);
    vi.mocked(api.getCodexVersion).mockResolvedValue(
      { version: "0.142.5", minRequired: "0.144.0", ok: false } as any);
    openModel();
    await screen.findByLabelText("Model codex");
    expect(screen.queryByTestId("codex-version-note")).toBeNull();
  });
});
```

- [x] **Step 2: Jalankan test, pastikan GAGAL**

Run: `env -u NODE_ENV DATABASE_URL=postgresql://hanoman:hanoman@localhost:5432/hanoman339 pnpm vitest run --no-file-parallelism src/test/codex-version-note.test.tsx`
Expected: FAIL — `codex-version-note` tak ditemukan.

- [x] **Step 3: Implementasi catatan**

Di `src/src/screens/SettingsScreen.tsx`, tambahkan `codexModel` ke impor `@hanoman/shared`, lalu di komponen yang merender tab (dekat `const codex = s.codex ?? CODEX_DEFAULTS;`) tambahkan state + fetch:

```tsx
  // SPEC-339 · versi codex CLI, untuk peringatan LUNAK saja. Gagal-diam: endpoint yang error tak
  // boleh membuat layar Settings gagal render.
  const [codexVer, setCodexVer] = React.useState<{ version: string | null; minRequired: string } | null>(null);
  React.useEffect(() => {
    api.getCodexVersion().then(setCodexVer).catch(() => {});
  }, []);
```

Tambahkan helper perbandingan versi di berkas yang sama, tepat di atas komponen:

```tsx
// SPEC-339 · perbandingan versi numerik per segmen (cermin cmpVersion di server). String compare
// salah: "0.9.0" akan dianggap LEBIH BARU dari "0.144.0".
function olderThan(have: string, need: string): boolean {
  const a = have.split(".").map(Number);
  const b = need.split(".").map(Number);
  for (let i = 0; i < Math.max(a.length, b.length); i++) {
    const d = (a[i] ?? 0) - (b[i] ?? 0);
    if (d !== 0) return d < 0;
  }
  return false;
}
```

Di dalam `<Card eyebrow="agen" title="Agen sesi">`, tepat sebelum `SettingRow "Model codex"`, sisipkan blok berikut. Peringatan hanya muncul bila SEMUA syaratnya terpenuhi: versi benar-benar terdeteksi, model terpilih memang menuntut klien lebih baru, dan versinya memang lebih rendah.

```tsx
        {(() => {
          const need = codexModel(codex.model)?.minClient;
          const have = codexVer?.version;
          if (!need || !have || !olderThan(have, need)) return null;
          return (
            <div data-testid="codex-version-note" style={{
              fontSize: 12, lineHeight: 1.5, marginBottom: 10, padding: "8px 10px",
              borderRadius: 8, background: "var(--warn-bg, #fdf6e3)", color: "var(--text-muted)",
            }}>
              Codex CLI terpasang <b>{have}</b>, sedangkan <b>{codex.model}</b> butuh <b>{need}</b>.
              Sesi tetap boleh dijalankan, tapi modelnya belum tentu dikenali CLI ini.
              Perbarui dengan <code>npm i -g @openai/codex@latest</code>.
            </div>
          );
        })()}
```

- [x] **Step 4: Jalankan test, pastikan LULUS**

Run: `env -u NODE_ENV DATABASE_URL=postgresql://hanoman:hanoman@localhost:5432/hanoman339 pnpm vitest run --no-file-parallelism src/test/codex-version-note.test.tsx`
Expected: PASS (4 test).

- [x] **Step 5: Perbaiki mock yang kini kurang lengkap**

`settings-agent.test.tsx` memock modul `../src/api/client` secara utuh; komponen kini memanggil `api.getCodexVersion` juga. Tambahkan ke mock-nya:

```ts
vi.mock("../src/api/client", () => ({
  api: { getSettings: vi.fn(), putSettings: vi.fn(), getCodexVersion: vi.fn() },
  ApiError: class extends Error { status = 0 },
}));
```

dan di `beforeEach`:

```ts
  vi.mocked(api.getCodexVersion).mockResolvedValue({ version: "0.145.0", minRequired: "0.144.0", ok: true } as any);
```

Run: `env -u NODE_ENV DATABASE_URL=postgresql://hanoman:hanoman@localhost:5432/hanoman339 pnpm vitest run --no-file-parallelism src`
Expected: seluruh test `src` PASS.

- [x] **Step 6: Commit**

```bash
git add src/src/screens/SettingsScreen.tsx src/test/codex-version-note.test.tsx src/test/settings-agent.test.tsx
git commit -m "feat(spec-339): catatan lunak versi codex CLI di Settings"
```

---

### Task 7: Docs, suite penuh, dan smoke API nyata

**Files:**
- Modify: `internal/docs/adr/0074-codex-sebagai-mesin-sesi.md`
- Modify: `internal/docs/architecture/stack.md`
- Modify: `internal/docs/architecture/api-contract.md`
- Modify: `internal/skills/hanoman/SKILL.md`
- Modify: `internal/docs/README.md` (bila daftar isinya menyebut berkas yang berubah)

- [x] **Step 1: Perbarui docs**

Di `internal/docs/adr/0074-codex-sebagai-mesin-sesi.md`, tambahkan catatan di bagian konsekuensi:

```markdown
## Pembaruan SPEC-339 (2026-07-27) — katalog per-model

GPT-5.6 (`gpt-5.6-sol`, `gpt-5.6-terra`, `gpt-5.6-luna`) menambahkan effort `max` dan `ultra`, dan
dukungannya TAK seragam: Luna tak mendukung `ultra`, dan seluruh model 5.5 ke bawah tak mendukung
keduanya. Karena itu `CODEX_MODELS` kini membawa `efforts`/`fallback`/`minClient` per model, dan
`CODEX_EFFORTS` hanya bertahan sebagai gabungan (bukan sumber pilihan UI). Koersi effort dilakukan
di `createSession` — satu titik yang dilewati SEMUA kelahiran sesi. Trio 5.6 butuh codex CLI
>= 0.144.0; `GET /api/codex/version` memberi peringatan lunak, tanpa memblokir.
```

Di `internal/docs/architecture/api-contract.md`, tambahkan pada daftar endpoint:

```markdown
- `GET /api/codex/version` → `{ version: string | null, minRequired: "0.144.0", ok: boolean }`
  (SPEC-339) — versi codex CLI terpasang. `version: null` = tak terdeteksi, dan itu TIDAK dianggap
  gagal (`ok: true`). Observabilitas untuk peringatan model GPT-5.6; tak pernah memblokir Start.
```

Di `internal/docs/architecture/stack.md` dan `internal/skills/hanoman/SKILL.md`, perbarui setiap
penyebutan katalog/model codex agar menyebut trio 5.6, effort `max`/`ultra`, dan syarat CLI 0.144.0.

- [x] **Step 2: Jalankan SELURUH suite**

Run:
```bash
env -u NODE_ENV DATABASE_URL=postgresql://hanoman:hanoman@localhost:5432/hanoman339 \
  pnpm test
```
Expected: seluruh paket PASS. Perbaiki yang merah sebelum lanjut — jangan lanjut ke smoke di atas suite merah.

- [x] **Step 3: Typecheck seluruh workspace**

Run: `pnpm typecheck`
Expected: exit 0, tanpa error.

- [x] **Step 4: Smoke API nyata (wajib per CLAUDE.md)**

Boot server terhadap DB sekali pakai lalu curl endpoint yang tersentuh:

```bash
docker exec hanoman-db-1 psql -U hanoman -d postgres -c 'CREATE DATABASE hanoman339_smoke'
DATABASE_URL=postgresql://hanoman:hanoman@localhost:5432/hanoman339_smoke \
  pnpm --filter ./server exec prisma migrate deploy
pnpm --filter ./server build
DATABASE_URL=postgresql://hanoman:hanoman@localhost:5432/hanoman339_smoke PORT=8799 \
  node server/dist/server.js &
```

Jangan pakai port 8787 (ada sesi dev lain dengan kode & DB berbeda). Buat akun + cookie sesuai pola
smoke sebelumnya, lalu:

```bash
curl -s localhost:8799/api/codex/version -b /tmp/c339.txt
# Expect: {"version":"0.145.0","minRequired":"0.144.0","ok":true}

curl -s localhost:8799/api/settings -b /tmp/c339.txt | python3 -m json.tool | grep -A2 codex
# Expect: model gpt-5.6-sol, effort xhigh

curl -s -X PUT localhost:8799/api/settings -b /tmp/c339.txt \
  -H 'content-type: application/json' \
  -d '{"model":"claude-opus-5","effort":"xhigh","autoDefault":true,"autoScaffold":true,"notifyFail":true,"notifyDone":true,"notifySound":"short","notifyDecision":true,"notifyDecisionSound":"alert","agent":"codex","codex":{"model":"gpt-5.6-luna","effort":"ultra"}}'
curl -s localhost:8799/api/settings -b /tmp/c339.txt | python3 -m json.tool | grep -A2 codex
# Expect: model gpt-5.6-luna, effort XHIGH — bukan ultra (koersi bekerja lewat jalur HTTP nyata)
```

**Jangan** `POST /terminal/sessions` dalam smoke: itu men-spawn codex sungguhan.

Matikan server smoke setelah selesai (`kill %1`) dan hapus DB smoke-nya.

- [x] **Step 5: Commit docs**

```bash
git add internal/docs/adr/0074-codex-sebagai-mesin-sesi.md internal/docs/architecture/stack.md \
        internal/docs/architecture/api-contract.md internal/skills/hanoman/SKILL.md internal/docs/README.md
git commit -m "docs(spec-339): katalog codex per-model + endpoint versi CLI"
```

- [x] **Step 6: Push branch**

```bash
git push -u origin hanoman/spec-339
```

Jangan merge ke `main` tanpa instruksi — konvensi repo ini: push branch, integrasi diputuskan manusia.

---

## Catatan lingkungan

- Codex CLI di mesin dev sudah di-upgrade ke **0.145.0** saat spec ditulis; trio 5.6 terkonfirmasi
  muncul di `codex debug models`. **VPS belum** — bila VPS menjalankan sesi codex, ia perlu upgrade
  yang sama sebelum default `gpt-5.6-sol` berguna di sana.
- `~/.codex/models_cache.json` menyimpan katalog beserta `client_version`. Bila ragu apakah sebuah
  slug benar-benar dilayani, `codex debug models` adalah jawaban otoritatifnya — bukan ingatan,
  bukan blog.

---

## Catatan eksekusi (2026-07-27)

Semua task selesai; suite penuh **232 berkas / 1599 test lulus**, `pnpm typecheck` exit 0, smoke API
nyata lulus 5/5. Enam penyimpangan dari rencana, semuanya sudah dikoreksi di atas atau di kode:

1. **Perintah test `shared` di rencana salah.** `env -u DATABASE_URL` membuat seluruh run mati saat
   startup: vitest workspace memuat `server/vitest.config.ts` walau yang dijalankan hanya `shared`,
   dan config itu menuntut `DATABASE_URL`. Perintah di atas sudah diperbaiki ke base `hanoman339`.
2. **`buildApp({ requireAuth: false })`, bukan `build()`.** Sesuai antisipasi Task 4 Step 6 — pola
   `server/test/app.test.ts` yang dipakai. Karena gate auth bisa dimatikan, assert route jadi tegas
   (200 + bentuk persis), bukan `[200, 401]`.
3. **Catatan versi juga dipasang di picker Start**, tidak hanya Settings. Spec memang menyebut kedua
   permukaan; Task 6 semula hanya mencakup Settings — celah antara spec dan rencana, ditutup.
4. **`cmpVersion` + `codexClientTooOld` naik ke `shared`.** Rencana menaruh helper `olderThan` lokal
   di `SettingsScreen`, yang akan menjadikan perbandingan versi hidup di tiga tempat begitu picker
   Start ikut memakainya. Aturan peringatan kini satu implementasi; service server meng-ekspor ulang
   `cmpVersion` agar permukaannya tak berubah.
5. **Enam berkas test lain ikut disentuh.** `config-panel`, `settings-goal`, `settings-nav`,
   `settings-no-matrix`, `start-session-goal`, `start-session-model` memock modul `api` secara utuh;
   begitu komponen memanggil `api.getCodexVersion()`, mock tanpa method itu melempar. Ditambahkan
   satu baris per berkas.
6. **Dua test server menyemai `gpt-5.4`.** `session-launch.test.ts` dan `cross-audit-session.test.ts`
   meng-assert `-m gpt-5.4` di argv; slug itu kini SENGAJA diremap ke `gpt-5.5` saat dibaca, jadi
   fixture-nya dipindahkan ke `gpt-5.6-terra`. Kegagalannya benar, bukan regresi.

Hasil smoke (server nyata di :8799, DB sekali pakai, `GET`/`PUT /api/settings`):

| Dikirim | Dibaca kembali | Yang dibuktikan |
|---|---|---|
| `gpt-5.6-luna` + `ultra` | `gpt-5.6-luna` + `xhigh` | Luna tak punya `ultra` |
| `gpt-5.5` + `max` | `gpt-5.5` + `xhigh` | 5.5 tak punya `max` |
| `gpt-5.3-codex-spark` + `high` | `gpt-5.5` + `high` | pensiun, effort tetap dijaga |
| `gpt-5.6-sol` + `ultra` | `gpt-5.6-sol` + `ultra` | kombinasi sah tak diutak-atik |
| `gpt-7-belum-ada` + `ultra` | `gpt-7-belum-ada` + `ultra` | model asing diteruskan apa adanya |

`GET /api/codex/version` terhadap biner asli → `{"version":"0.145.0","minRequired":"0.144.0","ok":true}`.
