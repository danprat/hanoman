# Requirements — rekap

Tautan silang: business (`business/brd.md`) → produk (`entrypoints/prd.md`, `requirements/prd.md`) → arsitektur (`architecture/**`). Menjaga jejak dari "kenapa" ke "bagaimana".
